#!/usr/bin/env bash
# Prefer the project venv (it has claude-agent-sdk and pyyaml); fall back to python3.
here="$(cd "$(dirname "$0")" && pwd)"
if [ -x "$here/.venv/bin/python" ]; then
  exec "$here/.venv/bin/python" "$here/harness/cli.py" "$@"
fi
exec python3 "$here/harness/cli.py" "$@"
