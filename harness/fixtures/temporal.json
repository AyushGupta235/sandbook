{
  "calls": [
    {
      "stage": "curriculum",
      "system": "You design interactive technical lessons. You return JSON only.",
      "prompt": "# Curriculum stage\n\nYou design the outline for one interactive lesson. A later stage builds each\nmodule; your job is the shape of the whole thing.\n\n## What makes a good lesson here\n\nThe learner manipulates something and sees what happens. Reading is the\nconnective tissue, not the point. Aim the lesson at what someone gets wrong in\npractice, not at a definition list.\n\nPick the misconceptions first and build modules that force them into the open.\nA good module makes the learner commit to a belief and then shows them the\nconsequence. \"Explain what X is\" is a weak module; \"predict what happens when X\nand Y conflict, then watch it play out\" is a strong one.\n\nAssume the reader is a competent engineer meeting this specific topic properly\nfor the first time. Do not explain what a function or a network is.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation** in any text\nyou write, including the title, the subtitle, and every teaching note. Use a\ncomma, a colon, a semicolon, brackets, or two sentences.\n\n## Widget types\n\nEach module gets exactly one widget. Choose the one that fits what the learner\nshould do:\n\n- `param-playground`: sliders and dropdowns bound to a computation, redrawn\n  live. Use when the lesson is about how a quantity responds to inputs.\n- `predict-reveal`: the learner commits to a prediction before seeing the\n  answer. Use for a misconception you can make concrete and checkable. The\n  correct option is computed at runtime, never written down, so the question\n  must have an answer a function can produce.\n- `step-sim`: step through an algorithm or process one state at a time. Use\n  when the insight is in the sequence, and when the run reaches a definite end.\n- `code-cell`: the learner writes something real. Two flavours: Python code\n  checked by hidden assertions, or a text artefact (YAML, HCL, a config) that a\n  Python function parses and grades. Use when doing beats watching.\n\nA five-module lesson using four different widget types is usually right. Repeat\na widget type only when the second use earns it.\n\n## Output\n\nReturn one JSON object and nothing else. No markdown fence, no commentary.\n\n```\n{\n  \"slug\": \"kebab-case-identifier\",\n  \"title\": \"Short title\",\n  \"subtitle\": \"One sentence on what the learner will be able to do.\",\n  \"targets\": \"version or scope this is accurate for, or null\",\n  \"packages\": [\"pyyaml\"],\n  \"objectives\": [\"...\"],\n  \"misconceptions\": [{\"claim\": \"what people believe\", \"reality\": \"what is true\"}],\n  \"modules\": [\n    {\n      \"id\": \"kebab-id\",\n      \"title\": \"Module title\",\n      \"widget_type\": \"param-playground | predict-reveal | step-sim | code-cell\",\n      \"intent\": \"What the learner should understand after this module.\",\n      \"teaching_note\": \"The concrete thing to show, including specific numbers, \"\n                       \"presets, or scenarios the builder should use.\"\n    }\n  ]\n}\n```\n\nRules:\n\n- 4 to 6 modules.\n- `packages` lists Python packages the lesson's functions need, and must be\n  available in Pyodide. Use `[]` unless a package is genuinely required;\n  `pyyaml` for YAML parsing is the common exception. Do not list numpy unless\n  the maths needs it.\n- `teaching_note` is the highest-leverage field you write. Be specific: name\n  the presets, the values, the scenario. The builder follows it literally.\n- Set `targets` when the topic has versioned behaviour that could drift, such\n  as a tool or an API. Otherwise null.\n\n## Topic\n\nTemporal workflow determinism: what makes a workflow replay-safe, and how retry policies actually behave\n\nNo grounding notes supplied.\n",
      "reply": "{\"slug\":\"temporal-determinism-and-retries\",\"title\":\"Replay-Safe Temporal Workflows\",\"subtitle\":\"Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\",\"targets\":\"Temporal Server 1.2x with the Python SDK (temporalio 1.x); command/event semantics are stable across recent versions\",\"packages\":[],\"objectives\":[\"Explain replay as command-versus-history matching, not as running the code again from scratch\",\"Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\",\"Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\",\"Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\",\"Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\"],\"misconceptions\":[{\"claim\":\"Determinism means the workflow must not touch anything random or time-based.\",\"reality\":\"It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\"},{\"claim\":\"Replay re-executes activities, so replay is expensive and side effects happen twice.\",\"reality\":\"Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\"},{\"claim\":\"Any edit to a running workflow's code breaks it.\",\"reality\":\"Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\"},{\"claim\":\"maximumAttempts is what stops a retrying activity.\",\"reality\":\"Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\"},{\"claim\":\"The retry interval is the gap between the start of one attempt and the start of the next.\",\"reality\":\"The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\"}],\"modules\":[{\"id\":\"what-replay-actually-does\",\"title\":\"What Replay Actually Does\",\"widget_type\":\"step-sim\",\"intent\":\"Replay is workflow code re-executing while the SDK feeds recorded results back at each await point, matching newly emitted commands against existing history events.\",\"teaching_note\":\"Simulate a short workflow: charge = await charge_card(order), then await workflow.sleep(timedelta(hours=1)), then await send_receipt(charge), then return. Show two parallel columns at every step: the event history (rows like WorkflowExecutionStarted, ActivityTaskScheduled charge_card, ActivityTaskCompleted result=txn_881, TimerStarted 1h, TimerFired, ActivityTaskScheduled send_receipt) and the worker's state (current line of code, commands emitted this task, whether it is replaying or live). Walk 10 to 12 steps covering a worker crash after TimerFired and a fresh worker picking the task up. At the crash-recovery steps make the key point visually explicit: charge_card is NOT called again, its recorded result txn_881 is handed straight back, and an is_replaying flag reads True until history is exhausted, then flips False. End with the workflow completing so the sim has a definite terminal state.\"},{\"id\":\"which-line-breaks-replay\",\"title\":\"Which Line Breaks Replay\",\"widget_type\":\"predict-reveal\",\"intent\":\"Non-determinism is about whether an operation's result is recorded in history, not about whether the operation feels impure.\",\"teaching_note\":\"Present six one-line snippets inside workflow code and ask the learner to tag each SAFE or UNSAFE before revealing: (1) random.randint(1, 100), (2) workflow.random().randint(1, 100), (3) datetime.now(), (4) workflow.now(), (5) for k in some_dict: await act(k) where some_dict is a plain dict built in-workflow, (6) requests.get(url) directly in the workflow. Reveal must state the deterministic replacement and the reason: uuid, random and now are recorded as marker or side-effect events when taken from workflow.uuid4, workflow.random and workflow.now; dict iteration order is insertion-ordered in Python 3.7+ so (5) is actually SAFE, which is the surprise item; a raw HTTP call is unsafe not because of I/O purity but because its result is never written to history and it blocks the workflow task. Score the six tags at runtime. Close with a note that the failure surfaces as a NonDeterminismError at replay time, sometimes days after deploy, not at the original run.\"},{\"id\":\"safe-edit-or-broken-deploy\",\"title\":\"Safe Edit or Broken Deploy\",\"widget_type\":\"predict-reveal\",\"intent\":\"Only changes that alter the command sequence relative to already-recorded history break a running execution, and patching lets old and new histories both replay.\",\"teaching_note\":\"Fix one in-flight execution whose history already contains: charge_card scheduled and completed, a 1h timer fired, send_receipt scheduled and completed, and the workflow now blocked on a signal. Present five candidate deploys and ask the learner to predict BREAKS or SAFE for each: (a) renaming a local variable and adding log lines, (b) changing the retry policy on send_receipt, (c) inserting a new await fraud_check(order) before charge_card, (d) appending await archive(order) after the signal wait, (e) swapping the 1h timer for a 2h timer. Reveal that (a) and (b) are safe, (d) is safe because that code has not run yet for this execution, (c) breaks because command 1 becomes fraud_check where history has charge_card, and (e) is the subtle one: the timer duration is part of the recorded StartTimer command, so replay mismatches. Close by showing the patched form of (c) using workflow.patched('add-fraud-check') and explaining that the patch marker is itself written to history, which is what lets old executions take the old branch forever.\"},{\"id\":\"reading-a-retry-schedule\",\"title\":\"Reading a Retry Schedule\",\"widget_type\":\"param-playground\",\"intent\":\"A retry policy's four fields determine an exact, computable timeline, and maximumInterval flattens exponential growth much earlier than people expect.\",\"teaching_note\":\"Sliders for initialInterval (0.1s to 60s, default 1s), backoffCoefficient (1.0 to 4.0, default 2.0), maximumInterval (1s to 600s, default 100x initialInterval), maximumAttempts (0 meaning unlimited, up to 20, default 5), plus an attempt-duration slider (0s to 30s, default 3s) representing how long each failing attempt runs before erroring. Render a table of attempts with columns attempt number, wall-clock start, interval before it, cumulative elapsed; plus a bar chart of intervals showing where maximumInterval clips the curve. Presets: 'SDK default' (1s, 2.0, 100s, unlimited), 'Fast idempotent call' (0.1s, 2.0, 5s, 10), 'Slow external API' (5s, 2.0, 300s, 8), 'Linear' (10s, 1.0, 100s, 5) to show that coefficient 1.0 gives fixed intervals. Highlight two readings: with defaults the intervals go 1, 2, 4, 8, 16, 32, 64, 100, 100, so growth stops at attempt 8; and total elapsed includes attempt durations, so 5 attempts at 3s each with default backoff spans about 27s, not 15s.\"},{\"id\":\"which-bound-fires-first\",\"title\":\"Which Bound Fires First\",\"widget_type\":\"step-sim\",\"intent\":\"startToClose, scheduleToClose, maximumAttempts and nonRetryableErrorTypes are four independent stop conditions, and the tightest one wins.\",\"teaching_note\":\"Step through a failing activity on a timeline with config startToClose=10s, scheduleToClose=30s, initialInterval=2s, backoffCoefficient=2.0, maximumAttempts=10, nonRetryableErrorTypes=['InvalidCardError']. Each step advances the clock and shows attempt number, elapsed since schedule, and remaining budget against every bound. Scenario A: each attempt hangs and is killed by startToClose at 10s, so attempt 1 spans 0 to 10s, wait 2s, attempt 2 spans 12 to 22s, wait 4s, attempt 3 starts at 26s and scheduleToClose kills the whole thing at 30s mid-attempt; final failure is a ScheduleToClose TimeoutError after 3 attempts, not 10. Toggle for Scenario B where attempt 2 raises InvalidCardError, halting retries at about 12s with the application error surfaced. Toggle for Scenario C with scheduleToClose unset so the sequence runs all 10 attempts. Close by making explicit that reaching maximumAttempts is the exception, not the rule.\"},{\"id\":\"write-a-replay-safe-workflow\",\"title\":\"Make This Workflow Replay-Safe\",\"widget_type\":\"code-cell\",\"intent\":\"Turn recognition into production: rewrite a broken workflow using the deterministic SDK surface and an explicit retry policy.\",\"teaching_note\":\"Give a starter workflow function with four seeded defects: idem_key = str(uuid.uuid4()), an if datetime.now().hour < 9 branch, a time.sleep(5), and an activity call with no retry policy and no timeouts. Provide stub shims in the cell so it runs in Pyodide: a workflow module object exposing uuid4(), now(), random(), sleep(seconds) and execute_activity(fn, arg, start_to_close, retry_policy), plus a RetryPolicy dataclass. The learner rewrites the function. Hidden assertions run the function twice against a fake recorded history and assert the emitted command sequence is identical across both runs, assert no banned symbol (random., datetime.now, time.sleep, uuid.uuid4) appears in the submitted source, and assert the activity call carries a start_to_close timeout and a RetryPolicy with maximum_attempts set. Failure messages must name the specific defect, for example 'run 1 emitted idem_key=a3f... but run 2 emitted 91c...: the key must come from workflow.uuid4()'.\"}]}"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: what-replay-actually-does\ntitle: What Replay Actually Does\nwidget type: step-sim\nintent: Replay is workflow code re-executing while the SDK feeds recorded results back at each await point, matching newly emitted commands against existing history events.\nteaching note: Simulate a short workflow: charge = await charge_card(order), then await workflow.sleep(timedelta(hours=1)), then await send_receipt(charge), then return. Show two parallel columns at every step: the event history (rows like WorkflowExecutionStarted, ActivityTaskScheduled charge_card, ActivityTaskCompleted result=txn_881, TimerStarted 1h, TimerFired, ActivityTaskScheduled send_receipt) and the worker's state (current line of code, commands emitted this task, whether it is replaying or live). Walk 10 to 12 steps covering a worker crash after TimerFired and a fresh worker picking the task up. At the crash-recovery steps make the key point visually explicit: charge_card is NOT called again, its recorded result txn_881 is handed straight back, and an is_replaying flag reads True until history is exhausted, then flips False. End with the workflow completing so the sim has a definite terminal state.\n\nFunction names already used elsewhere in this lesson: (none yet)\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"Replay is not Temporal running your workflow function again from scratch as if nothing had happened. It is the SDK re-executing your code while quietly feeding it answers from a log of events the server already recorded, the *event history*. At every `await` on an activity or timer, the worker checks: is there already a matching event in history? If yes, it hands back the recorded result and moves on without doing anything in the real world. Only once the history runs out does the worker start emitting genuinely new commands.\\n\\nThis is why a worker crash is a non-event for correctness. A brand new worker process, with zero memory of anything, can pick up the task, replay the whole history from line one, and land in exactly the same place the crashed worker was in, without re-charging a card or re-sending an email. Step through the simulation below and watch the `is_replaying` flag: it is `True` while the worker is catching up to recorded history, and flips to `False` the instant it starts producing new commands.\\n\\nWatch closely at the crash: `charge_card` is called exactly once across the whole run, even though the workflow function's `charge = await charge_card(order)` line executes on three separate occasions (once live, twice during replay by two different workers).\",\n  \"widget\": {\n    \"type\": \"step-sim\",\n    \"title\": \"Replay: Same Code, Two Different Workers, One History\",\n    \"max_steps\": 9,\n    \"autoplay_ms\": 1200,\n    \"init\": {\"fn\": \"replay_sim_init\", \"args\": {}},\n    \"step\": {\"fn\": \"replay_sim_step\", \"args\": {\"state\": {\"state\": true}}},\n    \"view\": {\"fn\": \"replay_sim_view\", \"args\": {\"state\": {\"state\": true}}}\n  },\n  \"functions\": [\n    {\n      \"name\": \"replay_script_data\",\n      \"source\": \"def replay_script_data():\\n    e1 = \\\"WorkflowExecutionStarted\\\"\\n    e2 = \\\"ActivityTaskScheduled(charge_card)\\\"\\n    e3 = \\\"ActivityTaskCompleted(charge_card) -> txn_881\\\"\\n    e4 = \\\"TimerStarted(1h)\\\"\\n    e5 = \\\"TimerFired\\\"\\n    e6 = \\\"ActivityTaskScheduled(send_receipt)\\\"\\n    e7 = \\\"ActivityTaskCompleted(send_receipt) -> ok\\\"\\n    e8 = \\\"WorkflowExecutionCompleted\\\"\\n    return [\\n        {\\n            \\\"history\\\": [e1, e2],\\n            \\\"line\\\": \\\"charge = await charge_card(order)\\\",\\n            \\\"commands_emitted\\\": [\\\"ActivityTaskScheduled(charge_card)\\\"],\\n            \\\"is_replaying\\\": False,\\n            \\\"worker\\\": \\\"W1\\\",\\n            \\\"note\\\": \\\"Task 1: fresh execution, nothing recorded yet to replay against. The worker runs the code live and emits ActivityTaskScheduled for charge_card as a brand new command.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3],\\n            \\\"line\\\": \\\"charge = await charge_card(order)\\\",\\n            \\\"commands_emitted\\\": [],\\n            \\\"is_replaying\\\": True,\\n            \\\"worker\\\": \\\"W1\\\",\\n            \\\"note\\\": \\\"Task 2 starts because charge_card finished. The worker replays from the top: it reaches the charge_card call, finds ActivityTaskScheduled already in history, emits no new command, and reads txn_881 straight out of the recorded ActivityTaskCompleted event instead of calling the activity again.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4],\\n            \\\"line\\\": \\\"await workflow.sleep(timedelta(hours=1))\\\",\\n            \\\"commands_emitted\\\": [\\\"TimerStarted(1h)\\\"],\\n            \\\"is_replaying\\\": False,\\n            \\\"worker\\\": \\\"W1\\\",\\n            \\\"note\\\": \\\"Replay runs out of recorded events right here. There is nothing left to match, so the worker flips to live and emits a genuinely new command: TimerStarted for one hour.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5],\\n            \\\"line\\\": \\\"await workflow.sleep(timedelta(hours=1))\\\",\\n            \\\"commands_emitted\\\": [],\\n            \\\"is_replaying\\\": False,\\n            \\\"worker\\\": \\\"W1\\\",\\n            \\\"note\\\": \\\"An hour passes, the timer fires, and TimerFired is appended to history on the server. Worker W1 crashes immediately after this, before it can start the next task. The history is unaffected: it lives on the server, not on the worker.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5],\\n            \\\"line\\\": \\\"charge = await charge_card(order)\\\",\\n            \\\"commands_emitted\\\": [],\\n            \\\"is_replaying\\\": True,\\n            \\\"worker\\\": \\\"W2\\\",\\n            \\\"note\\\": \\\"A fresh worker, W2, picks up the workflow task. It has no memory of anything, so it replays from line one. It reaches charge_card, matches ActivityTaskScheduled in history, and hands back txn_881 from the recorded completion. charge_card is NOT called a second time: no request goes out, no card is charged again.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5],\\n            \\\"line\\\": \\\"await workflow.sleep(timedelta(hours=1))\\\",\\n            \\\"commands_emitted\\\": [],\\n            \\\"is_replaying\\\": True,\\n            \\\"worker\\\": \\\"W2\\\",\\n            \\\"note\\\": \\\"W2 keeps replaying: TimerStarted and TimerFired are both already sitting in history, so the sleep resolves instantly during replay. No new commands, no real waiting.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5, e6],\\n            \\\"line\\\": \\\"await send_receipt(charge)\\\",\\n            \\\"commands_emitted\\\": [\\\"ActivityTaskScheduled(send_receipt)\\\"],\\n            \\\"is_replaying\\\": False,\\n            \\\"worker\\\": \\\"W2\\\",\\n            \\\"note\\\": \\\"Replay runs out of history exactly here. is_replaying flips from True to False and W2 goes live, emitting a real new command: ActivityTaskScheduled for send_receipt.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5, e6, e7],\\n            \\\"line\\\": \\\"await send_receipt(charge)\\\",\\n            \\\"commands_emitted\\\": [],\\n            \\\"is_replaying\\\": True,\\n            \\\"worker\\\": \\\"W2\\\",\\n            \\\"note\\\": \\\"send_receipt completes and a new task starts. W2 (still warm this time, no crash) replays up to the send_receipt call, matches it in history, and reads back the recorded result 'ok' instead of sending the receipt again.\\\",\\n        },\\n        {\\n            \\\"history\\\": [e1, e2, e3, e4, e5, e6, e7, e8],\\n            \\\"line\\\": \\\"return\\\",\\n            \\\"commands_emitted\\\": [\\\"WorkflowExecutionCompleted\\\"],\\n            \\\"is_replaying\\\": False,\\n            \\\"worker\\\": \\\"W2\\\",\\n            \\\"note\\\": \\\"Replay catches up one last time, flips live, and the final line emits WorkflowExecutionCompleted. The workflow is done. charge_card ran once and send_receipt ran once, total, across both workers and every replay.\\\",\\n        },\\n    ]\\n\"\n    },\n    {\n      \"name\": \"replay_sim_init\",\n      \"source\": \"def replay_sim_init():\\n    return {\\n        \\\"step\\\": 0,\\n        \\\"history\\\": [\\\"WorkflowExecutionStarted\\\"],\\n        \\\"line\\\": \\\"(workflow function entered)\\\",\\n        \\\"commands_emitted\\\": [],\\n        \\\"is_replaying\\\": False,\\n        \\\"worker\\\": \\\"W1\\\",\\n        \\\"note\\\": \\\"Task 1 begins. History holds one event so far, WorkflowExecutionStarted. The worker is about to run the workflow code live for the first time ever.\\\",\\n        \\\"done\\\": False,\\n    }\\n\"\n    },\n    {\n      \"name\": \"replay_sim_step\",\n      \"source\": \"def replay_sim_step(state):\\n    script = replay_script_data()\\n    idx = state.get(\\\"step\\\", 0)\\n    if idx >= len(script):\\n        new_state = dict(state)\\n        new_state[\\\"done\\\"] = True\\n        return new_state\\n    entry = script[idx]\\n    new_step = idx + 1\\n    new_state = dict(entry)\\n    new_state[\\\"step\\\"] = new_step\\n    new_state[\\\"done\\\"] = new_step >= len(script)\\n    return new_state\\n\"\n    },\n    {\n      \"name\": \"replay_sim_view\",\n      \"source\": \"def replay_sim_view(state):\\n    history = state.get(\\\"history\\\", [])\\n    line = state.get(\\\"line\\\", \\\"\\\")\\n    worker = state.get(\\\"worker\\\", \\\"\\\")\\n    is_replaying = state.get(\\\"is_replaying\\\", False)\\n    commands = state.get(\\\"commands_emitted\\\", [])\\n    note = state.get(\\\"note\\\", \\\"\\\")\\n    row_labels = [str(i + 1) for i in range(len(history))]\\n    cells = [[event] for event in history]\\n    commands_text = \\\", \\\".join(commands) if commands else \\\"none (matched an existing history event)\\\"\\n    state_text = (\\n        \\\"Worker \\\" + str(worker) + \\\" is at `\\\" + str(line) + \\\"`. is_replaying = \\\" +\\n        str(is_replaying) + \\\". Commands emitted this task: \\\" + commands_text + \\\".\\\"\\n    )\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"caption\\\": note,\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"grid\\\",\\n                \\\"row_labels\\\": row_labels,\\n                \\\"col_labels\\\": [\\\"Event\\\"],\\n                \\\"cells\\\": cells,\\n                \\\"caption\\\": \\\"The event history recorded on the Temporal server. Every worker, old or new, replays workflow code against exactly this list.\\\",\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": state_text,\\n                \\\"caption\\\": note,\\n            },\\n        ],\\n    }\\n\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: which-line-breaks-replay\ntitle: Which Line Breaks Replay\nwidget type: predict-reveal\nintent: Non-determinism is about whether an operation's result is recorded in history, not about whether the operation feels impure.\nteaching note: Present six one-line snippets inside workflow code and ask the learner to tag each SAFE or UNSAFE before revealing: (1) random.randint(1, 100), (2) workflow.random().randint(1, 100), (3) datetime.now(), (4) workflow.now(), (5) for k in some_dict: await act(k) where some_dict is a plain dict built in-workflow, (6) requests.get(url) directly in the workflow. Reveal must state the deterministic replacement and the reason: uuid, random and now are recorded as marker or side-effect events when taken from workflow.uuid4, workflow.random and workflow.now; dict iteration order is insertion-ordered in Python 3.7+ so (5) is actually SAFE, which is the surprise item; a raw HTTP call is unsafe not because of I/O purity but because its result is never written to history and it blocks the workflow task. Score the six tags at runtime. Close with a note that the failure surfaces as a NonDeterminismError at replay time, sometimes days after deploy, not at the original run.\n\nFunction names already used elsewhere in this lesson: replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"A workflow is deterministic if it emits the same sequence of commands (schedule this activity, start this timer, wait on this signal) every time its code runs against the same history. Replay is not \\\"run the code again from scratch\\\": it's the SDK re-executing your workflow function and checking each command it emits against what's already recorded. Activities are never re-scheduled on replay; their results are just served back from history the instant the code reaches that call.\\n\\nThat means the line that breaks replay isn't the one that \\\"feels\\\" impure, it's the one whose result never gets written to history in the first place. `random.randint` reseeds itself; `datetime.now()` reads the real clock; both produce a different value on replay than they did originally, and the command sequence downstream diverges. The SDK's `workflow.random()`, `workflow.now()`, and `workflow.uuid4()` exist specifically to fix this: they record their draw as a marker event the first time, and replay just reads that marker back.\\n\\nBelow are six one-line snippets pulled from workflow code. Tag each one SAFE or UNSAFE in your head, then pick the option that matches your tags. One of the six is a genuine trap either way you guess.\",\n  \"widget\": {\n    \"type\": \"predict-reveal\",\n    \"title\": \"Which Line Breaks Replay\",\n    \"question\": \"Six lines of workflow code are shown below, each doing something that might plausibly break replay.\\n\\n1. `random.randint(1, 100)`\\n2. `workflow.random().randint(1, 100)`\\n3. `datetime.now()`\\n4. `workflow.now()`\\n5. `for k in some_dict: await act(k)`  (some_dict is a plain dict built earlier in the same workflow run)\\n6. `requests.get(url)`  (called directly inside workflow code)\\n\\nExactly one of the four choices below correctly separates the three lines that cause a NonDeterminismError from the three that are safe as written. Which one?\",\n    \"options\": [\n      {\n        \"id\": \"a\",\n        \"text\": \"Unsafe: 1, 3, 6. Safe: 2, 4, 5.\",\n        \"predicate\": \"result == [True, False, True, False, False, True]\"\n      },\n      {\n        \"id\": \"b\",\n        \"text\": \"Unsafe: 1, 3, 5, 6. Safe: 2, 4.\",\n        \"predicate\": \"result == [True, False, True, False, True, True]\"\n      },\n      {\n        \"id\": \"c\",\n        \"text\": \"Unsafe: 1, 2, 3, 4, 6. Safe: 5.\",\n        \"predicate\": \"result == [True, True, True, True, False, True]\"\n      },\n      {\n        \"id\": \"d\",\n        \"text\": \"Unsafe: 1, 3. Safe: 2, 4, 5, 6.\",\n        \"predicate\": \"result == [True, False, True, False, False, False]\"\n      }\n    ],\n    \"check\": {\"fn\": \"classify_unsafe_lines\", \"args\": {}},\n    \"view\": {\"fn\": \"workflow_lines_view\", \"args\": {}},\n    \"explanation\": \"Line 1, `random.randint`, reseeds the stdlib generator on every process start, so replay draws a different number and the workflow's downstream commands diverge: unsafe. Line 2 swaps in `workflow.random()`, whose draw is written to history as a marker event the first time it runs; replay just reads that marker back: safe. Line 3, `datetime.now()`, reads the real wall clock, which is never the same value twice: unsafe. Line 4, `workflow.now()`, reads the time recorded in workflow history instead of the OS clock: safe. Line 5 is the trap: it looks non-deterministic because dict order feels arbitrary, but Python 3.7+ guarantees dicts iterate in insertion order, and that order was fixed when the dict was built earlier in the same deterministic run, so every replay walks it identically: safe. Line 6 calls out to the network directly from workflow code. The response is never recorded as a history event, so replay has nothing to play back, and the blocking call also stalls the workflow task; the fix is to move the call into an activity: unsafe. When one of these slips through, the failure doesn't show up at the original run. It shows up as a NonDeterminismError the next time that workflow replays, which can be days later, on a worker that never saw the first execution.\"\n  },\n  \"functions\": [\n    {\n      \"name\": \"classify_unsafe_lines\",\n      \"source\": \"def classify_unsafe_lines():\\n    # Order matches the six snippets shown in the question:\\n    # 1 random.randint, 2 workflow.random, 3 datetime.now, 4 workflow.now,\\n    # 5 dict iteration, 6 requests.get\\n    return [True, False, True, False, False, True]\"\n    },\n    {\n      \"name\": \"workflow_lines_view\",\n      \"source\": \"def workflow_lines_view():\\n    lines = [\\n        \\\"random.randint(1, 100)\\\",\\n        \\\"workflow.random().randint(1, 100)\\\",\\n        \\\"datetime.now()\\\",\\n        \\\"workflow.now()\\\",\\n        \\\"for k in some_dict: await act(k)\\\",\\n        \\\"requests.get(url)\\\",\\n    ]\\n    verdicts = [\\\"UNSAFE\\\", \\\"SAFE\\\", \\\"UNSAFE\\\", \\\"SAFE\\\", \\\"SAFE\\\", \\\"UNSAFE\\\"]\\n    reasons = [\\n        \\\"Stdlib random reseeds on every process start; replay draws a different value. Use workflow.random().\\\",\\n        \\\"workflow.random() records its draw as a marker event, so replay reads the same value back.\\\",\\n        \\\"Wall-clock time differs between the original run and any later replay. Use workflow.now().\\\",\\n        \\\"workflow.now() reads the time recorded in workflow history, so replay sees the same value.\\\",\\n        \\\"Python 3.7+ dicts preserve insertion order, and the dict was built earlier in the same deterministic run, so iteration order is identical on replay.\\\",\\n        \\\"The HTTP response is never written to history, and the call blocks the workflow task. Move it into an activity.\\\",\\n    ]\\n    cells = [[lines[i], verdicts[i], reasons[i]] for i in range(6)]\\n    return {\\n        \\\"kind\\\": \\\"grid\\\",\\n        \\\"row_labels\\\": [f\\\"Line {i + 1}\\\" for i in range(6)],\\n        \\\"col_labels\\\": [\\\"Code\\\", \\\"Verdict\\\", \\\"Why\\\"],\\n        \\\"cells\\\": cells,\\n        \\\"caption\\\": \\\"Three lines are non-deterministic because their result is never recorded in workflow history (1, 3, 6). The other three are safe: two because they use the SDK's replay-aware equivalents (2, 4), and one because Python's dict-ordering guarantee makes plain iteration deterministic all by itself (5).\\\",\\n    }\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: safe-edit-or-broken-deploy\ntitle: Safe Edit or Broken Deploy\nwidget type: predict-reveal\nintent: Only changes that alter the command sequence relative to already-recorded history break a running execution, and patching lets old and new histories both replay.\nteaching note: Fix one in-flight execution whose history already contains: charge_card scheduled and completed, a 1h timer fired, send_receipt scheduled and completed, and the workflow now blocked on a signal. Present five candidate deploys and ask the learner to predict BREAKS or SAFE for each: (a) renaming a local variable and adding log lines, (b) changing the retry policy on send_receipt, (c) inserting a new await fraud_check(order) before charge_card, (d) appending await archive(order) after the signal wait, (e) swapping the 1h timer for a 2h timer. Reveal that (a) and (b) are safe, (d) is safe because that code has not run yet for this execution, (c) breaks because command 1 becomes fraud_check where history has charge_card, and (e) is the subtle one: the timer duration is part of the recorded StartTimer command, so replay mismatches. Close by showing the patched form of (c) using workflow.patched('add-fraud-check') and explaining that the patch marker is itself written to history, which is what lets old executions take the old branch forever.\n\nFunction names already used elsewhere in this lesson: classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, workflow_lines_view\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"Replay is not \"run the code again and see what happens.\" It is a check: does this version of the workflow code, executed from the top, emit the same sequence of commands that the recorded history already contains, up to the point the execution had reached? Once you frame it that way, most edits are obviously fine, because most edits don't touch the command sequence at all.\\n\\nThe execution below has already scheduled and completed `charge_card`, fired a 1 hour timer, scheduled and completed `send_receipt`, and is now parked waiting on a signal. That's four commands already written into history: ScheduleActivity(charge_card), StartTimer(1h), ScheduleActivity(send_receipt), and the signal wait. Five candidate deploys are about to go out while this execution is still in flight. For each one, ask: does replaying the new code from the top still produce those same four commands, in that order, with the same parameters?\\n\\nWatch the timer especially closely. A `workflow.sleep` duration looks like configuration, but the SDK turns it into a `StartTimer` command with that duration baked in as an argument. Change the duration and you've changed the command, even though nothing about *what kind* of command it is has changed.\",\n  \"widget\": {\n    \"type\": \"predict-reveal\",\n    \"title\": \"Five deploys, one in-flight execution\",\n    \"question\": \"An execution has this history so far: charge_card scheduled and completed, a 1 hour timer fired, send_receipt scheduled and completed, now blocked on a signal. Five candidate deploys are about to ship while it's still running: (a) rename a local variable and add log lines, (b) change the retry policy on send_receipt, (c) insert a new `await fraud_check(order)` before `charge_card`, (d) append `await archive(order)` after the signal wait, (e) swap the 1 hour timer for a 2 hour timer. Which of these break replay for this execution?\",\n    \"options\": [\n      {\"id\": \"only_c\", \"text\": \"Only (c) breaks. It's the only one that changes what happens before the current point in history; the timer duration is just a config value, not part of the command.\", \"predicate\": \"result[2] == 'BREAKS' and sum(1 for x in result if x == 'BREAKS') == 1\"},\n      {\"id\": \"c_and_e\", \"text\": \"(c) and (e) break. Everything else is safe.\", \"predicate\": \"result[2] == 'BREAKS' and result[4] == 'BREAKS' and sum(1 for x in result if x == 'BREAKS') == 2\"},\n      {\"id\": \"all_break\", \"text\": \"All five break. You can't safely deploy code changes while an execution is in flight, period.\", \"predicate\": \"all(x == 'BREAKS' for x in result)\"},\n      {\"id\": \"none_break\", \"text\": \"None of them break, as long as you leave the retry policy alone; retry policy is the only thing replay actually checks.\", \"predicate\": \"all(x == 'SAFE' for x in result)\"}\n    ],\n    \"check\": {\"fn\": \"classify_deploys\", \"args\": {}},\n    \"view\": {\"fn\": \"deploy_verdicts_view\", \"args\": {}},\n    \"explanation\": \"(a) renaming a variable and adding logs changes nothing about which commands get emitted, so it's safe. (b) the retry policy only matters while an activity is actively retrying; it isn't itself a command in history, so changing it is safe too, and it only affects behavior the next time send_receipt runs. (d) appending `archive()` after the signal wait is safe for this execution specifically, because that code sits past the point history has reached; there is nothing recorded yet for replay to disagree with. It would only matter for a *new* execution starting after the deploy, where it just becomes a normal new activity call.\\n\\n(c) breaks: history's first command is ScheduleActivity(charge_card), but the new code's first command is now ScheduleActivity(fraud_check). Replay compares command-by-command against history and fails on mismatch number one.\\n\\n(e) also breaks, and it's the one people miss. `workflow.sleep(duration)` doesn't just start *a* timer, it starts a timer with that specific duration recorded as a StartTimer command argument. History says 1 hour; the new code says 2 hours. Same command type, different argument, still a mismatch.\\n\\nThe fix for (c) is `workflow.patched`: wrap the new call so old and new code paths both exist, and let a marker in history decide which one an execution takes.\\n\\n```python\\nif workflow.patched(\\\"add-fraud-check\\\"):\\n    await fraud_check(order)\\ncharge_card(order)\\n```\\n\\nOn replay, an execution that started before the patch never wrote the `add-fraud-check` marker to its history, so `patched()` returns False forever for that execution and it takes the old branch, matching what already happened. A brand new execution starting after the deploy writes the marker on its first pass and takes the new branch from then on. The marker itself is just another recorded event, which is what makes both histories replay cleanly at once.\"\n  },\n  \"functions\": [\n    {\n      \"name\": \"classify_deploys\",\n      \"source\": \"def classify_deploys():\\n    # Order matches the five candidate deploys in the lesson:\\n    # a) rename var + logs\\n    # b) change retry policy on send_receipt\\n    # c) insert fraud_check() before charge_card\\n    # d) append archive() after the signal wait\\n    # e) swap 1h timer for a 2h timer\\n    return [\\\"SAFE\\\", \\\"SAFE\\\", \\\"BREAKS\\\", \\\"SAFE\\\", \\\"BREAKS\\\"]\"\n    },\n    {\n      \"name\": \"deploy_verdicts_view\",\n      \"source\": \"def deploy_verdicts_view():\\n    labels = [\\n        \\\"a) rename local variable + add log lines\\\",\\n        \\\"b) change retry policy on send_receipt\\\",\\n        \\\"c) insert fraud_check() before charge_card\\\",\\n        \\\"d) append archive() after the signal wait\\\",\\n        \\\"e) swap 1h timer for a 2h timer\\\",\\n    ]\\n    verdicts = classify_deploys()\\n    cells = [[v] for v in verdicts]\\n    return {\\n        \\\"kind\\\": \\\"grid\\\",\\n        \\\"row_labels\\\": labels,\\n        \\\"column_labels\\\": [\\\"Verdict\\\"],\\n        \\\"cells\\\": cells,\\n        \\\"caption\\\": \\\"History so far: ScheduleActivity(charge_card) -> StartTimer(1h) -> ScheduleActivity(send_receipt) -> waiting on signal. Each row is a candidate deploy checked against that recorded sequence.\\\"\\n    }\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Claiming a kernel\n\nSome primitives have a trusted implementation checked into this repo. If one of\nyour functions computes one of them, say so:\n\n```\n{\"name\": \"kv_bytes\", \"source\": \"def kv_bytes(...):\\n    ...\",\n \"implements\": \"kv_cache_bytes\"}\n```\n\nYour function is then run against the kernel's own probe inputs and must agree\nwith it everywhere. Claim a kernel only when your function genuinely computes\nthat quantity, with the same argument names and the same return shape, which\nyou can read off the kernel's signature. A claim you cannot meet fails the\nmodule; no claim at all costs you nothing.\n\nAvailable kernels:\n\n{kernels}\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: reading-a-retry-schedule\ntitle: Reading a Retry Schedule\nwidget type: param-playground\nintent: A retry policy's four fields determine an exact, computable timeline, and maximumInterval flattens exponential growth much earlier than people expect.\nteaching note: Sliders for initialInterval (0.1s to 60s, default 1s), backoffCoefficient (1.0 to 4.0, default 2.0), maximumInterval (1s to 600s, default 100x initialInterval), maximumAttempts (0 meaning unlimited, up to 20, default 5), plus an attempt-duration slider (0s to 30s, default 3s) representing how long each failing attempt runs before erroring. Render a table of attempts with columns attempt number, wall-clock start, interval before it, cumulative elapsed; plus a bar chart of intervals showing where maximumInterval clips the curve. Presets: 'SDK default' (1s, 2.0, 100s, unlimited), 'Fast idempotent call' (0.1s, 2.0, 5s, 10), 'Slow external API' (5s, 2.0, 300s, 8), 'Linear' (10s, 1.0, 100s, 5) to show that coefficient 1.0 gives fixed intervals. Highlight two readings: with defaults the intervals go 1, 2, 4, 8, 16, 32, 64, 100, 100, so growth stops at attempt 8; and total elapsed includes attempt durations, so 5 attempts at 3s each with default backoff spans about 27s, not 15s.\n\nFunction names already used elsewhere in this lesson: classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, workflow_lines_view\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"A retry policy has four numbers, and once you fix them the whole schedule is determined: no randomness, no guessing. `initialInterval` sets the first wait, `backoffCoefficient` multiplies it each time, `maximumInterval` puts a ceiling on that growth, and `maximumAttempts` caps the count (0 means unlimited). The interval is measured *after* an attempt fails, so real elapsed time is attempt duration plus every interval before it, not the interval alone.\\n\\nPlay with the sliders and watch two things. First, exponential growth stops the moment `initialInterval * backoffCoefficient^k` exceeds `maximumInterval`; every attempt after that waits exactly the ceiling, however many attempts are left. Second, wall-clock time creeps up fast once attempts themselves take time: five attempts at 3 seconds each with the SDK defaults spans about 27 seconds, not 15.\\n\\nThe presets show the range in practice. A fast idempotent call clips its backoff almost immediately and burns through 10 attempts quickly. A slow external API with `scheduleToClose`-style limits often runs out of attempts long before backoff would have flattened. Linear backoff, `backoffCoefficient = 1.0`, is the one case where every interval is identical from the second attempt on.\",\n  \"widget\": {\n    \"type\": \"param-playground\",\n    \"title\": \"Retry Schedule Explorer\",\n    \"params\": [\n      {\"id\": \"initial_interval\", \"label\": \"Initial interval\", \"kind\": \"range\", \"min\": 0.1, \"max\": 60, \"step\": 0.1, \"default\": 1, \"format\": \".2f\", \"unit\": \"s\"},\n      {\"id\": \"backoff_coefficient\", \"label\": \"Backoff coefficient\", \"kind\": \"range\", \"min\": 1.0, \"max\": 4.0, \"step\": 0.05, \"default\": 2.0, \"format\": \".2f\", \"unit\": \"\"},\n      {\"id\": \"maximum_interval\", \"label\": \"Maximum interval\", \"kind\": \"range\", \"min\": 1, \"max\": 600, \"step\": 1, \"default\": 100, \"format\": \".0f\", \"unit\": \"s\"},\n      {\"id\": \"maximum_attempts\", \"label\": \"Maximum attempts (0 = unlimited)\", \"kind\": \"range\", \"min\": 0, \"max\": 20, \"step\": 1, \"default\": 5, \"format\": \".0f\", \"unit\": \"\"},\n      {\"id\": \"attempt_duration\", \"label\": \"Attempt duration before it fails\", \"kind\": \"range\", \"min\": 0, \"max\": 30, \"step\": 0.5, \"default\": 3, \"format\": \".1f\", \"unit\": \"s\"},\n      {\"id\": \"preset\", \"label\": \"Preset\", \"kind\": \"choice\", \"default\": \"custom\",\n       \"options\": [\n         {\"value\": \"custom\", \"label\": \"custom (use sliders)\"},\n         {\"value\": \"sdk_default\", \"label\": \"SDK default: 1s, 2.0x, 100s cap, unlimited\"},\n         {\"value\": \"fast_idempotent\", \"label\": \"Fast idempotent call: 0.1s, 2.0x, 5s cap, 10 attempts\"},\n         {\"value\": \"slow_external\", \"label\": \"Slow external API: 5s, 2.0x, 300s cap, 8 attempts\"},\n         {\"value\": \"linear\", \"label\": \"Linear: 10s, 1.0x, 100s cap, 5 attempts\"}\n       ]}\n    ],\n    \"view\": {\n      \"fn\": \"retry_stack_view\",\n      \"args\": {\n        \"preset\": {\"param\": \"preset\"},\n        \"initial_interval\": {\"param\": \"initial_interval\"},\n        \"backoff_coefficient\": {\"param\": \"backoff_coefficient\"},\n        \"maximum_interval\": {\"param\": \"maximum_interval\"},\n        \"maximum_attempts\": {\"param\": \"maximum_attempts\"},\n        \"attempt_duration\": {\"param\": \"attempt_duration\"}\n      }\n    },\n    \"readouts\": [\n      {\n        \"label\": \"Time the last shown attempt starts\",\n        \"fn\": \"total_elapsed_readout\",\n        \"format\": \".1f\",\n        \"unit\": \"s\",\n        \"args\": {\n          \"preset\": {\"param\": \"preset\"},\n          \"initial_interval\": {\"param\": \"initial_interval\"},\n          \"backoff_coefficient\": {\"param\": \"backoff_coefficient\"},\n          \"maximum_interval\": {\"param\": \"maximum_interval\"},\n          \"maximum_attempts\": {\"param\": \"maximum_attempts\"},\n          \"attempt_duration\": {\"param\": \"attempt_duration\"}\n        }\n      },\n      {\n        \"label\": \"First attempt clamped to maximumInterval\",\n        \"fn\": \"capped_at_attempt_readout\",\n        \"format\": \".0f\",\n        \"unit\": \"\",\n        \"args\": {\n          \"preset\": {\"param\": \"preset\"},\n          \"initial_interval\": {\"param\": \"initial_interval\"},\n          \"backoff_coefficient\": {\"param\": \"backoff_coefficient\"},\n          \"maximum_interval\": {\"param\": \"maximum_interval\"},\n          \"maximum_attempts\": {\"param\": \"maximum_attempts\"},\n          \"attempt_duration\": {\"param\": \"attempt_duration\"}\n        }\n      }\n    ]\n  },\n  \"functions\": [\n    {\n      \"name\": \"resolve_retry_config\",\n      \"source\": \"def resolve_retry_config(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts):\\n    presets = {\\n        \\\"sdk_default\\\": {\\\"initial_interval\\\": 1.0, \\\"backoff_coefficient\\\": 2.0, \\\"maximum_interval\\\": 100.0, \\\"maximum_attempts\\\": 0},\\n        \\\"fast_idempotent\\\": {\\\"initial_interval\\\": 0.1, \\\"backoff_coefficient\\\": 2.0, \\\"maximum_interval\\\": 5.0, \\\"maximum_attempts\\\": 10},\\n        \\\"slow_external\\\": {\\\"initial_interval\\\": 5.0, \\\"backoff_coefficient\\\": 2.0, \\\"maximum_interval\\\": 300.0, \\\"maximum_attempts\\\": 8},\\n        \\\"linear\\\": {\\\"initial_interval\\\": 10.0, \\\"backoff_coefficient\\\": 1.0, \\\"maximum_interval\\\": 100.0, \\\"maximum_attempts\\\": 5},\\n    }\\n    if preset in presets:\\n        p = presets[preset]\\n        return {\\n            \\\"initial_interval\\\": p[\\\"initial_interval\\\"],\\n            \\\"backoff_coefficient\\\": p[\\\"backoff_coefficient\\\"],\\n            \\\"maximum_interval\\\": p[\\\"maximum_interval\\\"],\\n            \\\"maximum_attempts\\\": p[\\\"maximum_attempts\\\"],\\n        }\\n    return {\\n        \\\"initial_interval\\\": initial_interval,\\n        \\\"backoff_coefficient\\\": backoff_coefficient,\\n        \\\"maximum_interval\\\": maximum_interval,\\n        \\\"maximum_attempts\\\": maximum_attempts,\\n    }\"\n    },\n    {\n      \"name\": \"retry_schedule\",\n      \"source\": \"def retry_schedule(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    cfg = resolve_retry_config(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts)\\n    ii = cfg[\\\"initial_interval\\\"]\\n    bc = cfg[\\\"backoff_coefficient\\\"]\\n    mi = cfg[\\\"maximum_interval\\\"]\\n    ma = cfg[\\\"maximum_attempts\\\"]\\n    n_attempts = ma if ma and ma > 0 else 20\\n    n_attempts = int(min(n_attempts, 20))\\n    if n_attempts < 1:\\n        n_attempts = 1\\n    attempt_numbers = []\\n    starts = []\\n    intervals = []\\n    cumulative = []\\n    capped_at = None\\n    start = 0.0\\n    for n in range(1, n_attempts + 1):\\n        if n == 1:\\n            interval = 0.0\\n        else:\\n            raw = ii * (bc ** (n - 2))\\n            interval = raw if raw < mi else mi\\n            if capped_at is None and raw >= mi:\\n                capped_at = n\\n            start = start + attempt_duration + interval\\n        attempt_numbers.append(n)\\n        starts.append(round(start, 3))\\n        intervals.append(round(interval, 3))\\n        cumulative.append(round(start + attempt_duration, 3))\\n    total_elapsed = starts[-1] if starts else 0.0\\n    return {\\n        \\\"attempt_numbers\\\": attempt_numbers,\\n        \\\"starts\\\": starts,\\n        \\\"intervals\\\": intervals,\\n        \\\"cumulative\\\": cumulative,\\n        \\\"total_elapsed\\\": round(total_elapsed, 3),\\n        \\\"n_attempts\\\": n_attempts,\\n        \\\"capped_at\\\": capped_at if capped_at is not None else n_attempts,\\n    }\"\n    },\n    {\n      \"name\": \"retry_table_view\",\n      \"source\": \"def retry_table_view(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    sched = retry_schedule(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    row_labels = [str(n) for n in sched[\\\"attempt_numbers\\\"]]\\n    col_labels = [\\\"Start (s)\\\", \\\"Interval before (s)\\\", \\\"Elapsed after (s)\\\"]\\n    cells = []\\n    for i in range(len(row_labels)):\\n        cells.append([sched[\\\"starts\\\"][i], sched[\\\"intervals\\\"][i], sched[\\\"cumulative\\\"][i]])\\n    caption = (\\n        \\\"Each row is one attempt. Start is the wall-clock time the attempt begins; interval before is the \\\"\\n        \\\"backoff wait after the previous attempt failed, not before it starts. Attempt \\\" + str(sched[\\\"n_attempts\\\"]) +\\n        \\\" begins at \\\" + str(sched[\\\"total_elapsed\\\"]) + \\\"s, which is attempt duration plus every prior interval, \\\"\\n        \\\"not just the sum of intervals.\\\"\\n    )\\n    return {\\n        \\\"kind\\\": \\\"grid\\\",\\n        \\\"row_labels\\\": row_labels,\\n        \\\"col_labels\\\": col_labels,\\n        \\\"cells\\\": cells,\\n        \\\"caption\\\": caption,\\n    }\"\n    },\n    {\n      \"name\": \"retry_bars_view\",\n      \"source\": \"def retry_bars_view(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    sched = retry_schedule(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    labels = [str(n) for n in sched[\\\"attempt_numbers\\\"]]\\n    values = sched[\\\"intervals\\\"]\\n    highlight = [i for i, n in enumerate(sched[\\\"attempt_numbers\\\"]) if n >= sched[\\\"capped_at\\\"]]\\n    caption = (\\n        \\\"Interval before each attempt. Growth is exponential until it hits maximumInterval, then it flattens: \\\"\\n        \\\"from attempt \\\" + str(sched[\\\"capped_at\\\"]) + \\\" onward every wait is clamped to the ceiling, however many \\\"\\n        \\\"attempts remain.\\\"\\n    )\\n    return {\\n        \\\"kind\\\": \\\"bars\\\",\\n        \\\"labels\\\": labels,\\n        \\\"values\\\": values,\\n        \\\"x_label\\\": \\\"Attempt\\\",\\n        \\\"y_label\\\": \\\"Interval before attempt (s)\\\",\\n        \\\"highlight\\\": highlight,\\n        \\\"caption\\\": caption,\\n    }\"\n    },\n    {\n      \"name\": \"retry_stack_view\",\n      \"source\": \"def retry_stack_view(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    table = retry_table_view(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    bars = retry_bars_view(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    return {\\\"kind\\\": \\\"stack\\\", \\\"panels\\\": [bars, table]}\"\n    },\n    {\n      \"name\": \"total_elapsed_readout\",\n      \"source\": \"def total_elapsed_readout(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    sched = retry_schedule(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    return sched[\\\"total_elapsed\\\"]\"\n    },\n    {\n      \"name\": \"capped_at_attempt_readout\",\n      \"source\": \"def capped_at_attempt_readout(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration):\\n    sched = retry_schedule(preset, initial_interval, backoff_coefficient, maximum_interval, maximum_attempts, attempt_duration)\\n    return sched[\\\"capped_at\\\"]\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Claiming a kernel\n\nSome primitives have a trusted implementation checked into this repo. If one of\nyour functions computes one of them, say so:\n\n```\n{\"name\": \"kv_bytes\", \"source\": \"def kv_bytes(...):\\n    ...\",\n \"implements\": \"kv_cache_bytes\"}\n```\n\nYour function is then run against the kernel's own probe inputs and must agree\nwith it everywhere. Claim a kernel only when your function genuinely computes\nthat quantity, with the same argument names and the same return shape, which\nyou can read off the kernel's signature. A claim you cannot meet fails the\nmodule; no claim at all costs you nothing.\n\nAvailable kernels:\n\n{kernels}\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: which-bound-fires-first\ntitle: Which Bound Fires First\nwidget type: step-sim\nintent: startToClose, scheduleToClose, maximumAttempts and nonRetryableErrorTypes are four independent stop conditions, and the tightest one wins.\nteaching note: Step through a failing activity on a timeline with config startToClose=10s, scheduleToClose=30s, initialInterval=2s, backoffCoefficient=2.0, maximumAttempts=10, nonRetryableErrorTypes=['InvalidCardError']. Each step advances the clock and shows attempt number, elapsed since schedule, and remaining budget against every bound. Scenario A: each attempt hangs and is killed by startToClose at 10s, so attempt 1 spans 0 to 10s, wait 2s, attempt 2 spans 12 to 22s, wait 4s, attempt 3 starts at 26s and scheduleToClose kills the whole thing at 30s mid-attempt; final failure is a ScheduleToClose TimeoutError after 3 attempts, not 10. Toggle for Scenario B where attempt 2 raises InvalidCardError, halting retries at about 12s with the application error surfaced. Toggle for Scenario C with scheduleToClose unset so the sequence runs all 10 attempts. Close by making explicit that reaching maximumAttempts is the exception, not the rule.\n\nFunction names already used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\"\n  ,\n  \"widget\": {\n    \"type\": \"step-sim\",\n    \"title\": \"Which Bound Fires First\",\n    \"max_steps\": 10,\n    \"autoplay_ms\": 900,\n    \"params\": [\n      {\n        \"id\": \"scenario\",\n        \"label\": \"Scenario\",\n        \"kind\": \"choice\",\n        \"default\": \"a\",\n        \"options\": [\n          {\"value\": \"a\", \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"},\n          {\"value\": \"b\", \"label\": \"B: a non-retryable InvalidCardError halts retries\"},\n          {\"value\": \"c\", \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"}\n        ]\n      }\n    ],\n    \"init\": {\"fn\": \"bound_sim_init\", \"args\": {\"scenario\": {\"param\": \"scenario\"}}},\n    \"step\": {\"fn\": \"bound_sim_step\", \"args\": {\"state\": {\"state\": true}}},\n    \"view\": {\"fn\": \"bound_sim_view\", \"args\": {\"state\": {\"state\": true}}}\n  },\n  \"functions\": [\n    {\n      \"name\": \"bound_sim_next_attempt\",\n      \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"\n    },\n    {\n      \"name\": \"bound_sim_init\",\n      \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_step\",\n      \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_view\",\n      \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "repair",
      "system": "You repair a rejected lesson module. You return JSON only.",
      "prompt": "# Repair stage\n\nA module you built was rejected by the verifier. Rebuild it so the reported\ndefects are gone.\n\nThe verifier is not advisory. Every finding below is a hard failure, and the\nmodule ships only when all of them are resolved.\n\n## How to approach this\n\nRead each finding and work out what is actually wrong before editing. Most\nfindings have a mechanical cause: a length mismatch, a name that does not\nresolve, a predicate that does not hold, a simulation that never sets `done`.\nFix the cause rather than working around the symptom.\n\nTwo failures deserve particular care because they usually mean the content is\nwrong rather than the wiring:\n\n- **Predicate count.** If zero or several option predicates hold, you asserted\n  numbers that the function does not produce. Compute the actual value, then\n  rewrite the options around it. Do not loosen a predicate until one happens to\n  match, which produces a question with a meaningless answer.\n- **Starter passes / solution fails.** The exercise is not doing its job. The\n  starter must be a plausible attempt that fails for the reason the module\n  teaches, and the solution must genuinely pass.\n\nReturn the whole module, not a patch. Keep everything that was not at fault,\nincluding prose and captions that were fine.\n\n## Writing\n\nAny text you rewrite follows the lesson's style rules. **Never use an em-dash\n(`\u2014`) or an en-dash (`\u2013`) as punctuation**, in prose, captions, option text,\nexplanations, hints, assertion messages, or grader notes. Use a comma, a colon,\na semicolon, brackets, or two sentences. Say things directly, with no\nthroat-clearing and no summary paragraph restating the point.\n\nIf the module you were given contains one of those dashes, remove it while you\nare in there, whether or not it was reported as a finding.\n\n## Findings\n\n- [ERROR] which-bound-fires-first/step-sim: argument 'scenario' binds to undeclared param 'scenario'\n\n## The module as you last built it\n\n{\n  \"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\",\n  \"widget\": {\n    \"type\": \"step-sim\",\n    \"title\": \"Which Bound Fires First\",\n    \"max_steps\": 10,\n    \"autoplay_ms\": 900,\n    \"params\": [\n      {\n        \"id\": \"scenario\",\n        \"label\": \"Scenario\",\n        \"kind\": \"choice\",\n        \"default\": \"a\",\n        \"options\": [\n          {\n            \"value\": \"a\",\n            \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"\n          },\n          {\n            \"value\": \"b\",\n            \"label\": \"B: a non-retryable InvalidCardError halts retries\"\n          },\n          {\n            \"value\": \"c\",\n            \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"\n          }\n        ]\n      }\n    ],\n    \"init\": {\n      \"fn\": \"bound_sim_init\",\n      \"args\": {\n        \"scenario\": {\n          \"param\": \"scenario\"\n        }\n      }\n    },\n    \"step\": {\n      \"fn\": \"bound_sim_step\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    },\n    \"view\": {\n      \"fn\": \"bound_sim_view\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    }\n  },\n  \"functions\": [\n    {\n      \"name\": \"bound_sim_next_attempt\",\n      \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"\n    },\n    {\n      \"name\": \"bound_sim_init\",\n      \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_step\",\n      \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_view\",\n      \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"\n    }\n  ]\n}\n\n## Original brief\n\nid: which-bound-fires-first\ntitle: Which Bound Fires First\nwidget type: step-sim\nintent: startToClose, scheduleToClose, maximumAttempts and nonRetryableErrorTypes are four independent stop conditions, and the tightest one wins.\nteaching note: Step through a failing activity on a timeline with config startToClose=10s, scheduleToClose=30s, initialInterval=2s, backoffCoefficient=2.0, maximumAttempts=10, nonRetryableErrorTypes=['InvalidCardError']. Each step advances the clock and shows attempt number, elapsed since schedule, and remaining budget against every bound. Scenario A: each attempt hangs and is killed by startToClose at 10s, so attempt 1 spans 0 to 10s, wait 2s, attempt 2 spans 12 to 22s, wait 4s, attempt 3 starts at 26s and scheduleToClose kills the whole thing at 30s mid-attempt; final failure is a ScheduleToClose TimeoutError after 3 attempts, not 10. Toggle for Scenario B where attempt 2 raises InvalidCardError, halting retries at about 12s with the application error surfaced. Toggle for Scenario C with scheduleToClose unset so the sequence runs all 10 attempts. Close by making explicit that reaching maximumAttempts is the exception, not the rule.\n\nFunction names used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n## Output\n\nReturn one JSON object in the same shape as before, and nothing else:\n`{\"prose\": ..., \"widget\": ..., \"functions\": [...]}`\n",
      "reply": "{\"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\", \"widget\": {\"type\": \"step-sim\", \"title\": \"Which Bound Fires First\", \"max_steps\": 10, \"autoplay_ms\": 900, \"params\": [{\"id\": \"scenario\", \"name\": \"scenario\", \"label\": \"Scenario\", \"kind\": \"choice\", \"default\": \"a\", \"options\": [{\"value\": \"a\", \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"}, {\"value\": \"b\", \"label\": \"B: a non-retryable InvalidCardError halts retries\"}, {\"value\": \"c\", \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"}]}], \"init\": {\"fn\": \"bound_sim_init\", \"args\": {\"scenario\": {\"param\": \"scenario\"}}}, \"step\": {\"fn\": \"bound_sim_step\", \"args\": {\"state\": {\"state\": true}}}, \"view\": {\"fn\": \"bound_sim_view\", \"args\": {\"state\": {\"state\": true}}}}, \"functions\": [{\"name\": \"bound_sim_next_attempt\", \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"}, {\"name\": \"bound_sim_init\", \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"}, {\"name\": \"bound_sim_step\", \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"}, {\"name\": \"bound_sim_view\", \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"}]}"
    },
    {
      "stage": "repair",
      "system": "You repair a rejected lesson module. You return JSON only.",
      "prompt": "# Repair stage\n\nA module you built was rejected by the verifier. Rebuild it so the reported\ndefects are gone.\n\nThe verifier is not advisory. Every finding below is a hard failure, and the\nmodule ships only when all of them are resolved.\n\n## How to approach this\n\nRead each finding and work out what is actually wrong before editing. Most\nfindings have a mechanical cause: a length mismatch, a name that does not\nresolve, a predicate that does not hold, a simulation that never sets `done`.\nFix the cause rather than working around the symptom.\n\nTwo failures deserve particular care because they usually mean the content is\nwrong rather than the wiring:\n\n- **Predicate count.** If zero or several option predicates hold, you asserted\n  numbers that the function does not produce. Compute the actual value, then\n  rewrite the options around it. Do not loosen a predicate until one happens to\n  match, which produces a question with a meaningless answer.\n- **Starter passes / solution fails.** The exercise is not doing its job. The\n  starter must be a plausible attempt that fails for the reason the module\n  teaches, and the solution must genuinely pass.\n\nReturn the whole module, not a patch. Keep everything that was not at fault,\nincluding prose and captions that were fine.\n\n## Writing\n\nAny text you rewrite follows the lesson's style rules. **Never use an em-dash\n(`\u2014`) or an en-dash (`\u2013`) as punctuation**, in prose, captions, option text,\nexplanations, hints, assertion messages, or grader notes. Use a comma, a colon,\na semicolon, brackets, or two sentences. Say things directly, with no\nthroat-clearing and no summary paragraph restating the point.\n\nIf the module you were given contains one of those dashes, remove it while you\nare in there, whether or not it was reported as a finding.\n\n## Findings\n\n- [ERROR] which-bound-fires-first/step-sim: argument 'scenario' binds to undeclared param 'scenario'\n\n## The module as you last built it\n\n{\n  \"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\",\n  \"widget\": {\n    \"type\": \"step-sim\",\n    \"title\": \"Which Bound Fires First\",\n    \"max_steps\": 10,\n    \"autoplay_ms\": 900,\n    \"params\": [\n      {\n        \"id\": \"scenario\",\n        \"name\": \"scenario\",\n        \"label\": \"Scenario\",\n        \"kind\": \"choice\",\n        \"default\": \"a\",\n        \"options\": [\n          {\n            \"value\": \"a\",\n            \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"\n          },\n          {\n            \"value\": \"b\",\n            \"label\": \"B: a non-retryable InvalidCardError halts retries\"\n          },\n          {\n            \"value\": \"c\",\n            \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"\n          }\n        ]\n      }\n    ],\n    \"init\": {\n      \"fn\": \"bound_sim_init\",\n      \"args\": {\n        \"scenario\": {\n          \"param\": \"scenario\"\n        }\n      }\n    },\n    \"step\": {\n      \"fn\": \"bound_sim_step\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    },\n    \"view\": {\n      \"fn\": \"bound_sim_view\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    }\n  },\n  \"functions\": [\n    {\n      \"name\": \"bound_sim_next_attempt\",\n      \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"\n    },\n    {\n      \"name\": \"bound_sim_init\",\n      \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_step\",\n      \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_view\",\n      \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"\n    }\n  ]\n}\n\n## Original brief\n\nid: which-bound-fires-first\ntitle: Which Bound Fires First\nwidget type: step-sim\nintent: startToClose, scheduleToClose, maximumAttempts and nonRetryableErrorTypes are four independent stop conditions, and the tightest one wins.\nteaching note: Step through a failing activity on a timeline with config startToClose=10s, scheduleToClose=30s, initialInterval=2s, backoffCoefficient=2.0, maximumAttempts=10, nonRetryableErrorTypes=['InvalidCardError']. Each step advances the clock and shows attempt number, elapsed since schedule, and remaining budget against every bound. Scenario A: each attempt hangs and is killed by startToClose at 10s, so attempt 1 spans 0 to 10s, wait 2s, attempt 2 spans 12 to 22s, wait 4s, attempt 3 starts at 26s and scheduleToClose kills the whole thing at 30s mid-attempt; final failure is a ScheduleToClose TimeoutError after 3 attempts, not 10. Toggle for Scenario B where attempt 2 raises InvalidCardError, halting retries at about 12s with the application error surfaced. Toggle for Scenario C with scheduleToClose unset so the sequence runs all 10 attempts. Close by making explicit that reaching maximumAttempts is the exception, not the rule.\n\nFunction names used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n## Output\n\nReturn one JSON object in the same shape as before, and nothing else:\n`{\"prose\": ..., \"widget\": ..., \"functions\": [...]}`\n",
      "reply": "{\"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\", \"widget\": {\"type\": \"step-sim\", \"title\": \"Which Bound Fires First\", \"max_steps\": 10, \"autoplay_ms\": 900, \"params\": [{\"name\": \"scenario\", \"label\": \"Scenario\", \"kind\": \"choice\", \"default\": \"a\", \"options\": [{\"value\": \"a\", \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"}, {\"value\": \"b\", \"label\": \"B: a non-retryable InvalidCardError halts retries\"}, {\"value\": \"c\", \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"}]}], \"init\": {\"fn\": \"bound_sim_init\", \"args\": {\"scenario\": {\"param\": \"scenario\"}}}, \"step\": {\"fn\": \"bound_sim_step\", \"args\": {\"state\": {\"state\": true}}}, \"view\": {\"fn\": \"bound_sim_view\", \"args\": {\"state\": {\"state\": true}}}}, \"functions\": [{\"name\": \"bound_sim_next_attempt\", \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"}, {\"name\": \"bound_sim_init\", \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"}, {\"name\": \"bound_sim_step\", \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"}, {\"name\": \"bound_sim_view\", \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"}]}"
    },
    {
      "stage": "repair",
      "system": "You repair a rejected lesson module. You return JSON only.",
      "prompt": "# Repair stage\n\nA module you built was rejected by the verifier. Rebuild it so the reported\ndefects are gone.\n\nThe verifier is not advisory. Every finding below is a hard failure, and the\nmodule ships only when all of them are resolved.\n\n## How to approach this\n\nRead each finding and work out what is actually wrong before editing. Most\nfindings have a mechanical cause: a length mismatch, a name that does not\nresolve, a predicate that does not hold, a simulation that never sets `done`.\nFix the cause rather than working around the symptom.\n\nTwo failures deserve particular care because they usually mean the content is\nwrong rather than the wiring:\n\n- **Predicate count.** If zero or several option predicates hold, you asserted\n  numbers that the function does not produce. Compute the actual value, then\n  rewrite the options around it. Do not loosen a predicate until one happens to\n  match, which produces a question with a meaningless answer.\n- **Starter passes / solution fails.** The exercise is not doing its job. The\n  starter must be a plausible attempt that fails for the reason the module\n  teaches, and the solution must genuinely pass.\n\nReturn the whole module, not a patch. Keep everything that was not at fault,\nincluding prose and captions that were fine.\n\n## Writing\n\nAny text you rewrite follows the lesson's style rules. **Never use an em-dash\n(`\u2014`) or an en-dash (`\u2013`) as punctuation**, in prose, captions, option text,\nexplanations, hints, assertion messages, or grader notes. Use a comma, a colon,\na semicolon, brackets, or two sentences. Say things directly, with no\nthroat-clearing and no summary paragraph restating the point.\n\nIf the module you were given contains one of those dashes, remove it while you\nare in there, whether or not it was reported as a finding.\n\n## Findings\n\n- [ERROR] which-bound-fires-first/step-sim: argument 'scenario' binds to undeclared param 'scenario'\n\n## The module as you last built it\n\n{\n  \"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\",\n  \"widget\": {\n    \"type\": \"step-sim\",\n    \"title\": \"Which Bound Fires First\",\n    \"max_steps\": 10,\n    \"autoplay_ms\": 900,\n    \"params\": [\n      {\n        \"name\": \"scenario\",\n        \"label\": \"Scenario\",\n        \"kind\": \"choice\",\n        \"default\": \"a\",\n        \"options\": [\n          {\n            \"value\": \"a\",\n            \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"\n          },\n          {\n            \"value\": \"b\",\n            \"label\": \"B: a non-retryable InvalidCardError halts retries\"\n          },\n          {\n            \"value\": \"c\",\n            \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"\n          }\n        ]\n      }\n    ],\n    \"init\": {\n      \"fn\": \"bound_sim_init\",\n      \"args\": {\n        \"scenario\": {\n          \"param\": \"scenario\"\n        }\n      }\n    },\n    \"step\": {\n      \"fn\": \"bound_sim_step\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    },\n    \"view\": {\n      \"fn\": \"bound_sim_view\",\n      \"args\": {\n        \"state\": {\n          \"state\": true\n        }\n      }\n    }\n  },\n  \"functions\": [\n    {\n      \"name\": \"bound_sim_next_attempt\",\n      \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"\n    },\n    {\n      \"name\": \"bound_sim_init\",\n      \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_step\",\n      \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"\n    },\n    {\n      \"name\": \"bound_sim_view\",\n      \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"\n    }\n  ]\n}\n\n## Original brief\n\nid: which-bound-fires-first\ntitle: Which Bound Fires First\nwidget type: step-sim\nintent: startToClose, scheduleToClose, maximumAttempts and nonRetryableErrorTypes are four independent stop conditions, and the tightest one wins.\nteaching note: Step through a failing activity on a timeline with config startToClose=10s, scheduleToClose=30s, initialInterval=2s, backoffCoefficient=2.0, maximumAttempts=10, nonRetryableErrorTypes=['InvalidCardError']. Each step advances the clock and shows attempt number, elapsed since schedule, and remaining budget against every bound. Scenario A: each attempt hangs and is killed by startToClose at 10s, so attempt 1 spans 0 to 10s, wait 2s, attempt 2 spans 12 to 22s, wait 4s, attempt 3 starts at 26s and scheduleToClose kills the whole thing at 30s mid-attempt; final failure is a ScheduleToClose TimeoutError after 3 attempts, not 10. Toggle for Scenario B where attempt 2 raises InvalidCardError, halting retries at about 12s with the application error surfaced. Toggle for Scenario C with scheduleToClose unset so the sequence runs all 10 attempts. Close by making explicit that reaching maximumAttempts is the exception, not the rule.\n\nFunction names used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n## Output\n\nReturn one JSON object in the same shape as before, and nothing else:\n`{\"prose\": ..., \"widget\": ..., \"functions\": [...]}`\n",
      "reply": "{\"prose\": \"A retry policy has four stop conditions, and they are independent of each other: `startToClose` bounds a single attempt, `scheduleToClose` bounds the whole sequence from the first schedule, `maximumAttempts` bounds the attempt count, and `nonRetryableErrorTypes` can end everything on the very first attempt that raises one. None of them defers to the others. Whichever bound is crossed first wins, and the workflow sees whatever error that bound produces.\\n\\nThe scenario below fixes a realistic policy: `startToClose=10s`, `scheduleToClose=30s`, `initialInterval=2s`, `backoffCoefficient=2.0`, `maximumAttempts=10`, and `InvalidCardError` marked non-retryable. Step through Scenario A and watch an activity that always hangs: `startToClose` kills attempt 1 at 10s, the backoff waits 2s, attempt 2 runs 12 to 22s, backoff waits 4s, and attempt 3 starts at 26s only to be cut off mid-flight when `scheduleToClose` expires at 30s. That is three attempts total, not ten.\\n\\nSwitch to Scenario B and the second attempt raises `InvalidCardError` instead of hanging. Because that error type is non-retryable, the whole sequence stops at roughly 12 seconds, before `scheduleToClose` or `maximumAttempts` ever get a say. Switch to Scenario C, remove the `scheduleToClose` cap, and the same hanging activity now runs all the way to attempt 10, more than 18 minutes of elapsed time, before `maximumAttempts` finally ends it. That is the case `maximumAttempts` actually fires, and it is the exception, not the rule.\", \"widget\": {\"type\": \"step-sim\", \"title\": \"Which Bound Fires First\", \"max_steps\": 10, \"autoplay_ms\": 900, \"params\": [{\"name\": \"scenario\", \"label\": \"Scenario\", \"kind\": \"choice\", \"default\": \"a\", \"choices\": [{\"value\": \"a\", \"label\": \"A: startToClose kills each attempt, scheduleToClose ends the sequence\"}, {\"value\": \"b\", \"label\": \"B: a non-retryable InvalidCardError halts retries\"}, {\"value\": \"c\", \"label\": \"C: scheduleToClose unset, sequence runs to maximumAttempts\"}]}], \"init\": {\"fn\": \"bound_sim_init\", \"args\": {\"scenario\": {\"param\": \"scenario\"}}}, \"step\": {\"fn\": \"bound_sim_step\", \"args\": {\"state\": {\"state\": true}}}, \"view\": {\"fn\": \"bound_sim_view\", \"args\": {\"state\": {\"state\": true}}}}, \"functions\": [{\"name\": \"bound_sim_next_attempt\", \"source\": \"def bound_sim_next_attempt(scenario, attempts):\\n    STC = 10\\n    SCHED_TO_CLOSE = 30\\n    INITIAL_INTERVAL = 2\\n    BACKOFF = 2.0\\n    n = len(attempts) + 1\\n    if n == 1:\\n        start = 0.0\\n    else:\\n        prev = attempts[-1]\\n        wait = INITIAL_INTERVAL * (BACKOFF ** (prev[\\\"n\\\"] - 1))\\n        start = prev[\\\"end\\\"] + wait\\n    if scenario == \\\"b\\\" and n == 2:\\n        end = start\\n        outcome = \\\"InvalidCardError\\\"\\n        bound = \\\"nonRetryableErrorTypes\\\"\\n        retryable = False\\n    else:\\n        naive_end = start + STC\\n        sched_to_close = SCHED_TO_CLOSE if scenario in (\\\"a\\\", \\\"b\\\") else None\\n        if sched_to_close is not None and naive_end > sched_to_close:\\n            end = sched_to_close\\n            outcome = \\\"ScheduleToCloseTimeout\\\"\\n            bound = \\\"scheduleToClose\\\"\\n            retryable = False\\n        else:\\n            end = naive_end\\n            outcome = \\\"StartToCloseTimeout\\\"\\n            bound = \\\"startToClose\\\"\\n            retryable = True\\n    attempt = {\\n        \\\"n\\\": n,\\n        \\\"start\\\": round(start, 2),\\n        \\\"end\\\": round(end, 2),\\n        \\\"outcome\\\": outcome,\\n        \\\"bound\\\": bound,\\n    }\\n    return attempt, retryable\"}, {\"name\": \"bound_sim_init\", \"source\": \"def bound_sim_init(scenario):\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": [],\\n        \\\"step\\\": 0,\\n        \\\"done\\\": False,\\n        \\\"final_outcome\\\": None,\\n        \\\"final_bound\\\": None,\\n        \\\"elapsed\\\": 0.0,\\n    }\"}, {\"name\": \"bound_sim_step\", \"source\": \"def bound_sim_step(state):\\n    if state.get(\\\"done\\\"):\\n        return dict(state)\\n    scenario = state[\\\"scenario\\\"]\\n    attempts = list(state[\\\"attempts\\\"])\\n    attempt, retryable = bound_sim_next_attempt(scenario, attempts)\\n    new_attempts = attempts + [attempt]\\n    n = attempt[\\\"n\\\"]\\n    done = False\\n    final_outcome = None\\n    final_bound = None\\n    if not retryable:\\n        done = True\\n        final_outcome = attempt[\\\"outcome\\\"]\\n        final_bound = attempt[\\\"bound\\\"]\\n    elif n >= 10:\\n        done = True\\n        final_outcome = \\\"MaximumAttemptsExceeded\\\"\\n        final_bound = \\\"maximumAttempts\\\"\\n    return {\\n        \\\"scenario\\\": scenario,\\n        \\\"attempts\\\": new_attempts,\\n        \\\"step\\\": state[\\\"step\\\"] + 1,\\n        \\\"done\\\": done,\\n        \\\"final_outcome\\\": final_outcome,\\n        \\\"final_bound\\\": final_bound,\\n        \\\"elapsed\\\": attempt[\\\"end\\\"],\\n    }\"}, {\"name\": \"bound_sim_view\", \"source\": \"def bound_sim_view(state):\\n    attempts = state.get(\\\"attempts\\\", [])\\n    labels = [\\\"Attempt \\\" + str(a[\\\"n\\\"]) for a in attempts]\\n    durations = [round(a[\\\"end\\\"] - a[\\\"start\\\"], 2) for a in attempts]\\n    elapsed = state.get(\\\"elapsed\\\", 0.0)\\n    scenario = state.get(\\\"scenario\\\", \\\"a\\\")\\n    scenario_names = {\\n        \\\"a\\\": \\\"Scenario A: startToClose kills each attempt, scheduleToClose ends the sequence\\\",\\n        \\\"b\\\": \\\"Scenario B: a non-retryable InvalidCardError halts retries\\\",\\n        \\\"c\\\": \\\"Scenario C: scheduleToClose unset, sequence runs to maximumAttempts\\\",\\n    }\\n\\n    if not attempts:\\n        bars_caption = \\\"No attempts scheduled yet. Press step to schedule attempt 1.\\\"\\n    else:\\n        last = attempts[-1]\\n        bars_caption = (\\n            \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" spans \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n            \\\"s (\\\" + str(round(last[\\\"end\\\"] - last[\\\"start\\\"], 2)) + \\\"s), stopped by \\\" + last[\\\"bound\\\"] +\\n            \\\" with outcome \\\" + last[\\\"outcome\\\"] + \\\".\\\"\\n        )\\n\\n    items = [\\n        {\\\"label\\\": \\\"Elapsed since first schedule\\\", \\\"value\\\": round(elapsed, 2)},\\n        {\\\"label\\\": \\\"Attempts used\\\", \\\"value\\\": len(attempts)},\\n        {\\\"label\\\": \\\"Attempts remaining of max 10\\\", \\\"value\\\": max(0, 10 - len(attempts))},\\n    ]\\n    if scenario in (\\\"a\\\", \\\"b\\\"):\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": round(max(0.0, 30 - elapsed), 2)})\\n    else:\\n        items.append({\\\"label\\\": \\\"scheduleToClose budget remaining\\\", \\\"value\\\": -1})\\n\\n    if state.get(\\\"done\\\"):\\n        bound = state.get(\\\"final_bound\\\")\\n        outcome = state.get(\\\"final_outcome\\\")\\n        if bound == \\\"scheduleToClose\\\":\\n            text = (\\n                \\\"Workflow failed with ScheduleToCloseTimeoutError after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. The final attempt was cut off mid-flight \\\" +\\n                \\\"by the 30s scheduleToClose budget, far short of the 10-attempt maximumAttempts limit.\\\"\\n            )\\n        elif bound == \\\"nonRetryableErrorTypes\\\":\\n            text = (\\n                \\\"Workflow failed with an application error, InvalidCardError, after \\\" + str(len(attempts)) +\\n                \\\" attempts at \\\" + str(round(elapsed, 2)) + \\\"s elapsed. Retries stopped immediately because \\\" +\\n                \\\"InvalidCardError is listed in nonRetryableErrorTypes, so scheduleToClose and maximumAttempts \\\" +\\n                \\\"never got a chance to matter.\\\"\\n            )\\n        elif bound == \\\"maximumAttempts\\\":\\n            text = (\\n                \\\"Workflow failed after exhausting maximumAttempts=10 at \\\" + str(round(elapsed, 2)) +\\n                \\\"s elapsed. With scheduleToClose unset there was no time budget to cut things off early, so \\\" +\\n                \\\"maximumAttempts was the bound that actually fired here, which is the exception, not the rule.\\\"\\n            )\\n        else:\\n            text = \\\"Retry sequence finished: \\\" + str(outcome) + \\\".\\\"\\n    else:\\n        if not attempts:\\n            text = (\\n                \\\"Press step to schedule attempt 1 against startToClose=10s, scheduleToClose=\\\" +\\n                (\\\"30s\\\" if scenario in (\\\"a\\\", \\\"b\\\") else \\\"unset\\\") + \\\", maximumAttempts=10.\\\"\\n            )\\n        else:\\n            last = attempts[-1]\\n            text = (\\n                \\\"Attempt \\\" + str(last[\\\"n\\\"]) + \\\" ran from \\\" + str(last[\\\"start\\\"]) + \\\"s to \\\" + str(last[\\\"end\\\"]) +\\n                \\\"s and failed with \\\" + last[\\\"outcome\\\"] + \\\". The scheduler backs off before the next attempt.\\\"\\n            )\\n\\n    return {\\n        \\\"kind\\\": \\\"stack\\\",\\n        \\\"panels\\\": [\\n            {\\n                \\\"kind\\\": \\\"bars\\\",\\n                \\\"labels\\\": labels,\\n                \\\"values\\\": durations,\\n                \\\"x_label\\\": \\\"Attempt\\\",\\n                \\\"y_label\\\": \\\"Duration (s)\\\",\\n                \\\"y_min\\\": 0,\\n                \\\"y_max\\\": 10,\\n                \\\"caption\\\": bars_caption,\\n            },\\n            {\\n                \\\"kind\\\": \\\"scalars\\\",\\n                \\\"items\\\": items,\\n                \\\"caption\\\": scenario_names.get(scenario, \\\"Scenario\\\"),\\n            },\\n            {\\n                \\\"kind\\\": \\\"text\\\",\\n                \\\"text\\\": text,\\n            },\\n        ],\\n    }\"}]}"
    },
    {
      "stage": "module",
      "system": "You build one module of an interactive lesson. You return JSON only.",
      "prompt": "# Module builder stage\n\nYou build one module of a lesson: its prose, its widget configuration, and the\nPython functions the widget calls.\n\nEverything you write is checked by a verifier before it ships. The rules below\nare not style advice; a lesson that breaks them is rejected.\n\n## The two things you produce\n\n**Pure functions.** No I/O, no network, no global mutation, no unseeded\nrandomness. Arguments and return values are plain JSON data: lists, dicts,\nnumbers, strings, booleans. NaN and infinity are rejected at the boundary. The\nsame functions run in CPython during verification and in Pyodide in the\nbrowser, so they must not depend on either.\n\n**A widget configuration** that names those functions and binds their\narguments. It contains data only. You never write HTML, CSS, JavaScript, or any\ndrawing code.\n\n## View objects\n\nA function whose name ends in `_view` returns a description of what to draw.\nThe runtime does the drawing.\n\n| kind | required | notes |\n|---|---|---|\n| `bars` | `labels`, `values` | the two lists must be the same length |\n| `lines` | `x`, `series[].values`, `series[].label` | every series must match the length of `x`; the label is what the legend reads, and it is `label`, not `name` |\n| `grid` | `cells` | row and column label counts must match the cells |\n| `scalars` | `items[].value` | readouts and summary numbers |\n| `text` | `text` | a paragraph produced by a function |\n| `stack` | `panels` | composes any of the above |\n\nOptional anywhere: `caption`, `x_label`, `y_label`, `y_min`, `y_max`,\n`highlight` (indices), `value_format` (`\"d\"`, `\".3f\"`, `\".1%\"`).\n\nCaptions carry a lot of this lesson's teaching. Write them as sentences that\nsay what the learner is looking at and why it matters.\n\n## Argument bindings\n\n```\n{\"fn\": \"dist_view\", \"args\": {\n  \"preset\":      {\"param\": \"preset\"},   // current value of a control\n  \"temperature\": {\"param\": \"temperature\"},\n  \"top_p\":       {\"const\": 0.9},        // fixed value\n  \"state\":       {\"state\": true}        // a step-sim's current state\n}}\n```\n\n## Widget shapes\n\n**param-playground**\n```\n{\"type\": \"param-playground\", \"title\": \"...\",\n \"params\": [\n   {\"id\": \"temperature\", \"label\": \"Temperature\", \"kind\": \"range\",\n    \"min\": 0.05, \"max\": 3, \"step\": 0.05, \"default\": 1, \"format\": \".2f\", \"unit\": \"\"},\n   {\"id\": \"preset\", \"label\": \"Pattern\", \"kind\": \"choice\", \"default\": \"mixed\",\n    \"options\": [{\"value\": \"mixed\", \"label\": \"mixed: one clear leader\"}]}\n ],\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"readouts\": [{\"label\": \"...\", \"fn\": \"...\", \"format\": \".3f\", \"unit\": \"\", \"args\": {...}}]}\n```\nA range control needs numeric `min`, `max`, `default`, with the default inside\nthe range. A choice control's `default` must be one of its option values. The\nview function is called at the default setting and at every boundary of every\ncontrol, so it must not raise anywhere in that space.\n\n**predict-reveal**\n```\n{\"type\": \"predict-reveal\", \"title\": \"...\", \"question\": \"...\",\n \"options\": [{\"id\": \"a\", \"text\": \"...\", \"predicate\": \"max(result) > 0.95\"}],\n \"check\": {\"fn\": \"...\", \"args\": {...}},\n \"view\": {\"fn\": \"..._view\", \"args\": {...}},\n \"explanation\": \"...\"}\n```\n`check.fn` is called and each option's `predicate` is evaluated against the\nvalue it returned, bound to the name `result`. **Exactly one predicate must be\ntrue.** Never state which option is correct; a `correct` or `answer` field is\nitself a rejection. Predicates may use `max min sum abs len all any round\nsorted list range enumerate zip float int str bool` and `math`, and may index\ninto `result`.\n\nWork out the real numbers before writing the options. Every option must be\nsomething a competent person might actually believe, and the distractors must\nbe genuinely wrong once computed.\n\n**step-sim**\n```\n{\"type\": \"step-sim\", \"title\": \"...\", \"max_steps\": 5, \"autoplay_ms\": 900,\n \"init\": {\"fn\": \"..._init\", \"args\": {...}},\n \"step\": {\"fn\": \"..._step\", \"args\": {\"state\": {\"state\": true}}},\n \"view\": {\"fn\": \"..._view\", \"args\": {\"state\": {\"state\": true}}}}\n```\n`init` returns a state dict; `step` takes it and returns the next one; `view`\ndraws it. The state must carry `\"done\": true` on or before `max_steps`, or the\nlesson is rejected. `step` must not mutate the state it is given.\n\n**code-cell**, Python flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"python\", \"task\": \"...\",\n \"starter\": \"...\", \"solution\": \"...\", \"tests\": \"...\", \"hints\": [\"...\", \"...\"]}\n```\nThis is the one widget whose config names no function, which does **not** mean\nyou write none. Ship the reference implementation the tests check against, in\n`functions`, and have `tests` compare the learner's output to it:\n\n```python\n# in functions: the trusted version, under a different name\ndef softmax_probs(logits, temperature): ...\n\n# in tests: the learner's `softmax` is compared against it, case by case\ngot, want = softmax(logits, t), softmax_probs(logits, t)\nassert abs(got[0] - want[0]) < 1e-9, \"...\"\n```\n\nChecking against a reference beats hardcoding expected numbers: it covers more\ncases, and it cannot drift from what the rest of the lesson teaches.\n\nThe hidden `tests` run against the learner's code with the lesson's own model\nfunctions in scope, so a check can compare against a trusted implementation.\n**The solution must pass the tests and the starter must fail them.** A starter\nthat already passes is rejected. Make the starter a plausible, nearly-right\nattempt whose flaw is the lesson, not an empty stub. Assertion messages are\nteaching material: say what went wrong and what to reconsider.\n\n**code-cell**, graded flavour\n```\n{\"type\": \"code-cell\", \"title\": \"...\", \"language\": \"yaml\", \"task\": \"...\",\n \"grade\": {\"fn\": \"grade_...\"}, \"starter\": \"...\", \"solution\": \"...\", \"hints\": [...]}\n```\nThe learner's text is passed as a string to `grade.fn`, which parses and judges\nit. The text is data and is never executed. The grader returns:\n```\n{\"passed\": bool, \"message\": str,\n \"details\": [{\"label\": str, \"ok\": bool, \"note\": str}], \"view\": <view or null>}\n```\nSame rule: the solution passes, the starter fails. When it fails, at least one\nentry in `details` must have `ok: false`. A rejection with nothing marked\nwrong tells the learner nothing. Notes are only shown for failing checks, so\nwrite them as corrective guidance. Handle malformed input without raising.\n\nDeclare exactly one flavour. Setting both `tests` and `grade` is rejected.\n\n## Writing\n\nThis applies to every word a learner can see: prose, captions, option text,\nexplanations, hints, assertion messages, and grader notes.\n\n**Never use an em-dash (`\u2014`) or an en-dash (`\u2013`) as punctuation.** Use a comma,\na colon, a semicolon, brackets, or two sentences. A hyphen inside a compound\nword is fine.\n\nWrite the way a good engineer explains something to a colleague. Say the thing\ndirectly. No throat-clearing openers, no \"it's worth noting that\", no summary\nparagraph restating what you just said, no marketing adjectives, and no\nenthusiasm the material has not earned.\n\n## Claiming a kernel\n\nSome primitives have a trusted implementation checked into this repo. If one of\nyour functions computes one of them, say so:\n\n```\n{\"name\": \"kv_bytes\", \"source\": \"def kv_bytes(...):\\n    ...\",\n \"implements\": \"kv_cache_bytes\"}\n```\n\nYour function is then run against the kernel's own probe inputs and must agree\nwith it everywhere. Claim a kernel only when your function genuinely computes\nthat quantity, with the same argument names and the same return shape, which\nyou can read off the kernel's signature. A claim you cannot meet fails the\nmodule; no claim at all costs you nothing.\n\nAvailable kernels:\n\n{kernels}\n\n## Accuracy\n\nCompute anything you assert. If a caption or an explanation states a number,\nthat number must be what the function actually returns. State simplifications\nplainly in the module's prose or a caption rather than letting the learner\nbelieve the model is the real system.\n\n## Output\n\nReturn one JSON object and nothing else.\n\n```\n{\n  \"prose\": \"Markdown. Two or three short paragraphs setting up the widget. **bold**, *italic*, `code`, and lists are supported. Headings are not.\",\n  \"widget\": { ... },\n  \"functions\": [\n    {\"name\": \"exact_function_name\", \"source\": \"def exact_function_name(...):\\n    ...\"}\n  ]\n}\n```\n\n- Every function the widget names must appear in `functions`, and every entry's\n  `name` must match the `def` in its `source`.\n- **`functions` is never empty**, including for a code-cell whose widget config\n  names nothing. An empty list is rejected and the module is thrown away.\n- Function names must be unique across the whole lesson. Names already taken by\n  other modules are listed below; do not reuse them.\n- Shared helpers are fine. Put them in this module's `functions` and give them\n  names unlikely to collide.\n\n## Lesson\n\nTitle: Replay-Safe Temporal Workflows\nSubtitle: Predict how a Temporal workflow behaves on replay, and read a retry policy well enough to know exactly when the last attempt fires.\nObjectives:\n- Explain replay as command-versus-history matching, not as running the code again from scratch\n- Identify which operations inside workflow code are non-deterministic and name the deterministic SDK replacement for each\n- Distinguish a code change that is replay-safe from one that produces a non-determinism error, and apply patching to the unsafe case\n- Compute an exact retry schedule from initialInterval, backoffCoefficient, maximumInterval and maximumAttempts\n- Predict which of startToClose, scheduleToClose and maximumAttempts actually terminates a failing activity first\nMisconceptions this lesson targets:\n- believed: Determinism means the workflow must not touch anything random or time-based.  |  actually: It means the sequence of commands the code emits must be reproducible from history. Randomness and time are fine when they come from the SDK, because their results are recorded as events and replayed identically.\n- believed: Replay re-executes activities, so replay is expensive and side effects happen twice.  |  actually: Replay re-executes only workflow code. When the code reaches an activity call, the SDK serves the recorded result from history instead of scheduling anything. Side effects happen once.\n- believed: Any edit to a running workflow's code breaks it.  |  actually: Only edits that change the command sequence for already-recorded history break it. Renaming variables, changing logging, editing activity bodies and adding code after the current progress point are all safe.\n- believed: maximumAttempts is what stops a retrying activity.  |  actually: Whichever bound is hit first stops it. A scheduleToClose timeout routinely cuts a retry sequence off mid-backoff, long before the attempt count is exhausted, and an unset maximumAttempts means unlimited.\n- believed: The retry interval is the gap between the start of one attempt and the start of the next.  |  actually: The interval is measured after the previous attempt fails, so real elapsed time is attempt duration plus interval. With a 10s startToClose and a 1s interval, attempts land roughly 11s apart, not 1s apart.\n\n## This module\n\nid: write-a-replay-safe-workflow\ntitle: Make This Workflow Replay-Safe\nwidget type: code-cell\nintent: Turn recognition into production: rewrite a broken workflow using the deterministic SDK surface and an explicit retry policy.\nteaching note: Give a starter workflow function with four seeded defects: idem_key = str(uuid.uuid4()), an if datetime.now().hour < 9 branch, a time.sleep(5), and an activity call with no retry policy and no timeouts. Provide stub shims in the cell so it runs in Pyodide: a workflow module object exposing uuid4(), now(), random(), sleep(seconds) and execute_activity(fn, arg, start_to_close, retry_policy), plus a RetryPolicy dataclass. The learner rewrites the function. Hidden assertions run the function twice against a fake recorded history and assert the emitted command sequence is identical across both runs, assert no banned symbol (random., datetime.now, time.sleep, uuid.uuid4) appears in the submitted source, and assert the activity call carries a start_to_close timeout and a RetryPolicy with maximum_attempts set. Failure messages must name the specific defect, for example 'run 1 emitted idem_key=a3f... but run 2 emitted 91c...: the key must come from workflow.uuid4()'.\n\nFunction names already used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n\n",
      "reply": "```json\n{\n  \"prose\": \"A Temporal workflow is only replay-safe if every command it emits (start an activity, start a timer, ask the clock, generate an id) is *deterministic*: given the same history, the code must emit the same commands in the same order every time it runs. It doesn't need to avoid time, randomness or identity generation, it just needs to get them from the SDK instead of the standard library, because the SDK's versions are recorded in history and replayed from there instead of recomputed.\\n\\nBelow is a workflow function with four defects that are all variations on the same mistake: reaching past the SDK into `uuid`, `datetime` or `time` directly. Each one means a fresh worker process replaying this workflow from its recorded history could compute a different idempotency key, walk down the wrong branch, or block the thread on a sleep that isn't even a recorded event. There is a fifth, different kind of defect too: the activity call has no `start_to_close` timeout and no retry policy, so a stuck activity underneath it has nothing bounding how long, or how many times, it gets retried.\\n\\nRewrite `book_hotel_workflow` using the `workflow.*` shims already in the cell: `workflow.uuid4()`, `workflow.now()`, `workflow.sleep()`, and pass `start_to_close` and a `RetryPolicy(maximum_attempts=...)` into `workflow.execute_activity(...)`. Leave the shim classes and `confirm_booking` alone, they're the harness, not the exercise.\",\n  \"widget\": {\n    \"type\": \"code-cell\",\n    \"title\": \"Make This Workflow Replay-Safe\",\n    \"language\": \"python\",\n    \"task\": \"Fix `book_hotel_workflow` so that (1) it never touches `uuid`, `datetime.now()` or `time.sleep()` directly, using the `workflow.*` equivalents instead, and (2) its `execute_activity` call carries a `start_to_close` timeout and a `RetryPolicy` with `maximum_attempts` set. Do not change `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` or the `workflow` instance.\",\n    \"starter\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    # BUG 1: a fresh real id is different on every call, so replay would\\n    # recompute a different idempotency key than the one already in history.\\n    idem_key = str(uuid.uuid4())\\n\\n    # BUG 2: wall-clock time differs run to run, so this branch can take a\\n    # different path on replay than the one history says it took.\\n    if datetime.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    # BUG 3: a real sleep blocks the worker and is not a recorded event.\\n    time.sleep(0.05)\\n\\n    # BUG 4: no start_to_close timeout and no retry policy, so a stuck\\n    # activity has nothing bounding how long or how many times it retries.\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n    )\\n    return result\\n\",\n    \"solution\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    idem_key = str(workflow.uuid4())\\n\\n    if workflow.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    workflow.sleep(0.05)\\n\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n        start_to_close=10,\\n        retry_policy=RetryPolicy(\\n            initial_interval=1,\\n            backoff_coefficient=2,\\n            maximum_interval=30,\\n            maximum_attempts=5,\\n        ),\\n    )\\n    return result\\n\",\n    \"tests\": \"import inspect\\n\\n_src = inspect.getsource(book_hotel_workflow)\\n\\n_banned = [\\\"random.\\\", \\\"datetime.now\\\", \\\"time.sleep\\\", \\\"uuid.uuid4\\\"]\\nfor _b in _banned:\\n    assert _b not in _src, (\\n        f\\\"the function body still contains '{_b}': replace it with the matching \\\"\\n        f\\\"workflow.* call so replay reads the value from history instead of \\\"\\n        f\\\"recomputing it\\\"\\n    )\\n\\nrequest = {\\\"hotel\\\": \\\"riverside-inn\\\"}\\n\\nworkflow.log = []\\nresult_1 = book_hotel_workflow(request)\\nlog_1 = list(workflow.log)\\n\\nworkflow.log = []\\nresult_2 = book_hotel_workflow(request)\\nlog_2 = list(workflow.log)\\n\\naudit_1 = audit_command_log(log_1)\\naudit_2 = audit_command_log(log_2)\\n\\nassert audit_1[\\\"op_sequence\\\"] == audit_2[\\\"op_sequence\\\"], (\\n    f\\\"run 1 emitted commands {audit_1['op_sequence']} but run 2 emitted \\\"\\n    f\\\"{audit_2['op_sequence']}: the same recorded history must produce the \\\"\\n    f\\\"same sequence of commands on replay\\\"\\n)\\n\\nassert audit_1[\\\"idem_key\\\"] == audit_2[\\\"idem_key\\\"], (\\n    f\\\"run 1 emitted idem_key={audit_1['idem_key']} but run 2 emitted \\\"\\n    f\\\"idem_key={audit_2['idem_key']}: the key must come from workflow.uuid4(), \\\"\\n    f\\\"not uuid.uuid4()\\\"\\n)\\n\\nassert audit_1[\\\"has_activity\\\"], \\\"book_hotel_workflow never called workflow.execute_activity\\\"\\n\\nassert audit_1[\\\"has_timeout\\\"], (\\n    \\\"the activity call has no start_to_close timeout: a stuck activity would \\\"\\n    \\\"retry forever with nothing to cut it off\\\"\\n)\\n\\nassert audit_1[\\\"has_bounded_retries\\\"], (\\n    f\\\"maximum_attempts={audit_1['maximum_attempts']}: pass a RetryPolicy with \\\"\\n    f\\\"maximum_attempts set so the retries are bounded\\\"\\n)\\n\",\n    \"hints\": [\n      \"Swap the standard-library calls for the SDK shims: `uuid.uuid4()` becomes `workflow.uuid4()`, `datetime.now()` becomes `workflow.now()`, `time.sleep(...)` becomes `workflow.sleep(...)`. Nothing about what the code computes changes, only where the value comes from.\",\n      \"The `execute_activity` call needs two more keyword arguments: `start_to_close=<seconds>` and `retry_policy=RetryPolicy(maximum_attempts=<n>, ...)`. Without a timeout, a stuck activity has nothing bounding how long it runs or how many times it gets retried.\",\n      \"Leave `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` and the module-level `workflow = _FakeWorkflow()` exactly as given. Only `book_hotel_workflow`'s body should change.\",\n      \"Run the function in your head against the same recorded history twice: if any line can produce a different value the second time, that line is the bug.\"\n    ]\n  },\n  \"functions\": [\n    {\n      \"name\": \"audit_command_log\",\n      \"source\": \"def audit_command_log(commands):\\n    \\\"\\\"\\\"Inspect a captured list of workflow commands for replay-safety signals.\\n\\n    `commands` is a list of dicts, each shaped like one of:\\n      {\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"now\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": ...}\\n      {\\\"op\\\": \\\"execute_activity\\\", \\\"activity\\\": ..., \\\"arg\\\": {...},\\n       \\\"start_to_close\\\": number or None, \\\"maximum_attempts\\\": int or None}\\n\\n    Returns a plain dict summarizing the sequence so a caller can assert on\\n    it without re-walking the raw log.\\n    \\\"\\\"\\\"\\n    op_sequence = []\\n    has_activity = False\\n    start_to_close = None\\n    maximum_attempts = None\\n    idem_key = None\\n\\n    for entry in commands:\\n        if not isinstance(entry, dict):\\n            continue\\n        op = entry.get(\\\"op\\\")\\n        op_sequence.append(op)\\n        if op == \\\"execute_activity\\\":\\n            has_activity = True\\n            start_to_close = entry.get(\\\"start_to_close\\\")\\n            maximum_attempts = entry.get(\\\"maximum_attempts\\\")\\n            arg = entry.get(\\\"arg\\\")\\n            if isinstance(arg, dict):\\n                idem_key = arg.get(\\\"idem_key\\\")\\n\\n    has_bounded_retries = isinstance(maximum_attempts, int) and maximum_attempts > 0\\n\\n    return {\\n        \\\"op_sequence\\\": op_sequence,\\n        \\\"has_activity\\\": has_activity,\\n        \\\"start_to_close\\\": start_to_close,\\n        \\\"maximum_attempts\\\": maximum_attempts,\\n        \\\"idem_key\\\": idem_key,\\n        \\\"has_timeout\\\": start_to_close is not None,\\n        \\\"has_bounded_retries\\\": has_bounded_retries,\\n    }\"\n    }\n  ]\n}\n```"
    },
    {
      "stage": "repair",
      "system": "You repair a rejected lesson module. You return JSON only.",
      "prompt": "# Repair stage\n\nA module you built was rejected by the verifier. Rebuild it so the reported\ndefects are gone.\n\nThe verifier is not advisory. Every finding below is a hard failure, and the\nmodule ships only when all of them are resolved.\n\n## How to approach this\n\nRead each finding and work out what is actually wrong before editing. Most\nfindings have a mechanical cause: a length mismatch, a name that does not\nresolve, a predicate that does not hold, a simulation that never sets `done`.\nFix the cause rather than working around the symptom.\n\nTwo failures deserve particular care because they usually mean the content is\nwrong rather than the wiring:\n\n- **Predicate count.** If zero or several option predicates hold, you asserted\n  numbers that the function does not produce. Compute the actual value, then\n  rewrite the options around it. Do not loosen a predicate until one happens to\n  match, which produces a question with a meaningless answer.\n- **Starter passes / solution fails.** The exercise is not doing its job. The\n  starter must be a plausible attempt that fails for the reason the module\n  teaches, and the solution must genuinely pass.\n- **An argument binds to an undeclared param.** Only a param-playground has\n  controls. A step-sim has no `params`, so `{\"param\": \"...\"}` cannot resolve\n  there however reasonable the name looks. Pass the value to `init` as a\n  `{\"const\": ...}` and carry it in the state. Re-sending the same binding with\n  a different label will fail in exactly the same way.\n\nReturn the whole module, not a patch. Keep everything that was not at fault,\nincluding prose and captions that were fine.\n\n## Writing\n\nAny text you rewrite follows the lesson's style rules. **Never use an em-dash\n(`\u2014`) or an en-dash (`\u2013`) as punctuation**, in prose, captions, option text,\nexplanations, hints, assertion messages, or grader notes. Use a comma, a colon,\na semicolon, brackets, or two sentences. Say things directly, with no\nthroat-clearing and no summary paragraph restating the point.\n\nIf the module you were given contains one of those dashes, remove it while you\nare in there, whether or not it was reported as a finding.\n\n## Findings\n\n- [ERROR] write-a-replay-safe-workflow/code-cell: the provided solution does not pass its own checks: [tests] OSError: source code not available\n\n## The module as you last built it\n\n{\n  \"prose\": \"A Temporal workflow is only replay-safe if every command it emits (start an activity, start a timer, ask the clock, generate an id) is *deterministic*: given the same history, the code must emit the same commands in the same order every time it runs. It doesn't need to avoid time, randomness or identity generation, it just needs to get them from the SDK instead of the standard library, because the SDK's versions are recorded in history and replayed from there instead of recomputed.\\n\\nBelow is a workflow function with four defects that are all variations on the same mistake: reaching past the SDK into `uuid`, `datetime` or `time` directly. Each one means a fresh worker process replaying this workflow from its recorded history could compute a different idempotency key, walk down the wrong branch, or block the thread on a sleep that isn't even a recorded event. There is a fifth, different kind of defect too: the activity call has no `start_to_close` timeout and no retry policy, so a stuck activity underneath it has nothing bounding how long, or how many times, it gets retried.\\n\\nRewrite `book_hotel_workflow` using the `workflow.*` shims already in the cell: `workflow.uuid4()`, `workflow.now()`, `workflow.sleep()`, and pass `start_to_close` and a `RetryPolicy(maximum_attempts=...)` into `workflow.execute_activity(...)`. Leave the shim classes and `confirm_booking` alone, they're the harness, not the exercise.\",\n  \"widget\": {\n    \"type\": \"code-cell\",\n    \"title\": \"Make This Workflow Replay-Safe\",\n    \"language\": \"python\",\n    \"task\": \"Fix `book_hotel_workflow` so that (1) it never touches `uuid`, `datetime.now()` or `time.sleep()` directly, using the `workflow.*` equivalents instead, and (2) its `execute_activity` call carries a `start_to_close` timeout and a `RetryPolicy` with `maximum_attempts` set. Do not change `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` or the `workflow` instance.\",\n    \"starter\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    # BUG 1: a fresh real id is different on every call, so replay would\\n    # recompute a different idempotency key than the one already in history.\\n    idem_key = str(uuid.uuid4())\\n\\n    # BUG 2: wall-clock time differs run to run, so this branch can take a\\n    # different path on replay than the one history says it took.\\n    if datetime.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    # BUG 3: a real sleep blocks the worker and is not a recorded event.\\n    time.sleep(0.05)\\n\\n    # BUG 4: no start_to_close timeout and no retry policy, so a stuck\\n    # activity has nothing bounding how long or how many times it retries.\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n    )\\n    return result\\n\",\n    \"solution\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    idem_key = str(workflow.uuid4())\\n\\n    if workflow.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    workflow.sleep(0.05)\\n\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n        start_to_close=10,\\n        retry_policy=RetryPolicy(\\n            initial_interval=1,\\n            backoff_coefficient=2,\\n            maximum_interval=30,\\n            maximum_attempts=5,\\n        ),\\n    )\\n    return result\\n\",\n    \"tests\": \"import inspect\\n\\n_src = inspect.getsource(book_hotel_workflow)\\n\\n_banned = [\\\"random.\\\", \\\"datetime.now\\\", \\\"time.sleep\\\", \\\"uuid.uuid4\\\"]\\nfor _b in _banned:\\n    assert _b not in _src, (\\n        f\\\"the function body still contains '{_b}': replace it with the matching \\\"\\n        f\\\"workflow.* call so replay reads the value from history instead of \\\"\\n        f\\\"recomputing it\\\"\\n    )\\n\\nrequest = {\\\"hotel\\\": \\\"riverside-inn\\\"}\\n\\nworkflow.log = []\\nresult_1 = book_hotel_workflow(request)\\nlog_1 = list(workflow.log)\\n\\nworkflow.log = []\\nresult_2 = book_hotel_workflow(request)\\nlog_2 = list(workflow.log)\\n\\naudit_1 = audit_command_log(log_1)\\naudit_2 = audit_command_log(log_2)\\n\\nassert audit_1[\\\"op_sequence\\\"] == audit_2[\\\"op_sequence\\\"], (\\n    f\\\"run 1 emitted commands {audit_1['op_sequence']} but run 2 emitted \\\"\\n    f\\\"{audit_2['op_sequence']}: the same recorded history must produce the \\\"\\n    f\\\"same sequence of commands on replay\\\"\\n)\\n\\nassert audit_1[\\\"idem_key\\\"] == audit_2[\\\"idem_key\\\"], (\\n    f\\\"run 1 emitted idem_key={audit_1['idem_key']} but run 2 emitted \\\"\\n    f\\\"idem_key={audit_2['idem_key']}: the key must come from workflow.uuid4(), \\\"\\n    f\\\"not uuid.uuid4()\\\"\\n)\\n\\nassert audit_1[\\\"has_activity\\\"], \\\"book_hotel_workflow never called workflow.execute_activity\\\"\\n\\nassert audit_1[\\\"has_timeout\\\"], (\\n    \\\"the activity call has no start_to_close timeout: a stuck activity would \\\"\\n    \\\"retry forever with nothing to cut it off\\\"\\n)\\n\\nassert audit_1[\\\"has_bounded_retries\\\"], (\\n    f\\\"maximum_attempts={audit_1['maximum_attempts']}: pass a RetryPolicy with \\\"\\n    f\\\"maximum_attempts set so the retries are bounded\\\"\\n)\\n\",\n    \"hints\": [\n      \"Swap the standard-library calls for the SDK shims: `uuid.uuid4()` becomes `workflow.uuid4()`, `datetime.now()` becomes `workflow.now()`, `time.sleep(...)` becomes `workflow.sleep(...)`. Nothing about what the code computes changes, only where the value comes from.\",\n      \"The `execute_activity` call needs two more keyword arguments: `start_to_close=<seconds>` and `retry_policy=RetryPolicy(maximum_attempts=<n>, ...)`. Without a timeout, a stuck activity has nothing bounding how long it runs or how many times it gets retried.\",\n      \"Leave `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` and the module-level `workflow = _FakeWorkflow()` exactly as given. Only `book_hotel_workflow`'s body should change.\",\n      \"Run the function in your head against the same recorded history twice: if any line can produce a different value the second time, that line is the bug.\"\n    ]\n  },\n  \"functions\": [\n    {\n      \"name\": \"audit_command_log\",\n      \"source\": \"def audit_command_log(commands):\\n    \\\"\\\"\\\"Inspect a captured list of workflow commands for replay-safety signals.\\n\\n    `commands` is a list of dicts, each shaped like one of:\\n      {\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"now\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": ...}\\n      {\\\"op\\\": \\\"execute_activity\\\", \\\"activity\\\": ..., \\\"arg\\\": {...},\\n       \\\"start_to_close\\\": number or None, \\\"maximum_attempts\\\": int or None}\\n\\n    Returns a plain dict summarizing the sequence so a caller can assert on\\n    it without re-walking the raw log.\\n    \\\"\\\"\\\"\\n    op_sequence = []\\n    has_activity = False\\n    start_to_close = None\\n    maximum_attempts = None\\n    idem_key = None\\n\\n    for entry in commands:\\n        if not isinstance(entry, dict):\\n            continue\\n        op = entry.get(\\\"op\\\")\\n        op_sequence.append(op)\\n        if op == \\\"execute_activity\\\":\\n            has_activity = True\\n            start_to_close = entry.get(\\\"start_to_close\\\")\\n            maximum_attempts = entry.get(\\\"maximum_attempts\\\")\\n            arg = entry.get(\\\"arg\\\")\\n            if isinstance(arg, dict):\\n                idem_key = arg.get(\\\"idem_key\\\")\\n\\n    has_bounded_retries = isinstance(maximum_attempts, int) and maximum_attempts > 0\\n\\n    return {\\n        \\\"op_sequence\\\": op_sequence,\\n        \\\"has_activity\\\": has_activity,\\n        \\\"start_to_close\\\": start_to_close,\\n        \\\"maximum_attempts\\\": maximum_attempts,\\n        \\\"idem_key\\\": idem_key,\\n        \\\"has_timeout\\\": start_to_close is not None,\\n        \\\"has_bounded_retries\\\": has_bounded_retries,\\n    }\"\n    }\n  ]\n}\n\n## Original brief\n\nid: write-a-replay-safe-workflow\ntitle: Make This Workflow Replay-Safe\nwidget type: code-cell\nintent: Turn recognition into production: rewrite a broken workflow using the deterministic SDK surface and an explicit retry policy.\nteaching note: Give a starter workflow function with four seeded defects: idem_key = str(uuid.uuid4()), an if datetime.now().hour < 9 branch, a time.sleep(5), and an activity call with no retry policy and no timeouts. Provide stub shims in the cell so it runs in Pyodide: a workflow module object exposing uuid4(), now(), random(), sleep(seconds) and execute_activity(fn, arg, start_to_close, retry_policy), plus a RetryPolicy dataclass. The learner rewrites the function. Hidden assertions run the function twice against a fake recorded history and assert the emitted command sequence is identical across both runs, assert no banned symbol (random., datetime.now, time.sleep, uuid.uuid4) appears in the submitted source, and assert the activity call carries a start_to_close timeout and a RetryPolicy with maximum_attempts set. Failure messages must name the specific defect, for example 'run 1 emitted idem_key=a3f... but run 2 emitted 91c...: the key must come from workflow.uuid4()'.\n\nFunction names used elsewhere in this lesson: capped_at_attempt_readout, classify_unsafe_lines, replay_script_data, replay_sim_init, replay_sim_step, replay_sim_view, resolve_retry_config, retry_bars_view, retry_schedule, retry_stack_view, retry_table_view, total_elapsed_readout, workflow_lines_view\n\n## Output\n\nReturn one JSON object in the same shape as before, and nothing else:\n`{\"prose\": ..., \"widget\": ..., \"functions\": [...]}`\n",
      "reply": "{\"prose\": \"A Temporal workflow is only replay-safe if every command it emits (start an activity, start a timer, ask the clock, generate an id) is *deterministic*: given the same history, the code must emit the same commands in the same order every time it runs. It doesn't need to avoid time, randomness or identity generation, it just needs to get them from the SDK instead of the standard library, because the SDK's versions are recorded in history and replayed from there instead of recomputed.\\n\\nBelow is a workflow function with four defects that are all variations on the same mistake: reaching past the SDK into `uuid`, `datetime` or `time` directly. Each one means a fresh worker process replaying this workflow from its recorded history could compute a different idempotency key, walk down the wrong branch, or block the thread on a sleep that isn't even a recorded event. There is a fifth, different kind of defect too: the activity call has no `start_to_close` timeout and no retry policy, so a stuck activity underneath it has nothing bounding how long, or how many times, it gets retried.\\n\\nRewrite `book_hotel_workflow` using the `workflow.*` shims already in the cell: `workflow.uuid4()`, `workflow.now()`, `workflow.sleep()`, and pass `start_to_close` and a `RetryPolicy(maximum_attempts=...)` into `workflow.execute_activity(...)`. Leave the shim classes and `confirm_booking` alone, they're the harness, not the exercise.\", \"widget\": {\"type\": \"code-cell\", \"title\": \"Make This Workflow Replay-Safe\", \"language\": \"python\", \"task\": \"Fix `book_hotel_workflow` so that (1) it never touches `uuid`, `datetime.now()` or `time.sleep()` directly, using the `workflow.*` equivalents instead, and (2) its `execute_activity` call carries a `start_to_close` timeout and a `RetryPolicy` with `maximum_attempts` set. Do not change `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` or the `workflow` instance.\", \"starter\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    # BUG 1: a fresh real id is different on every call, so replay would\\n    # recompute a different idempotency key than the one already in history.\\n    idem_key = str(uuid.uuid4())\\n\\n    # BUG 2: wall-clock time differs run to run, so this branch can take a\\n    # different path on replay than the one history says it took.\\n    if datetime.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    # BUG 3: a real sleep blocks the worker and is not a recorded event.\\n    time.sleep(0.05)\\n\\n    # BUG 4: no start_to_close timeout and no retry policy, so a stuck\\n    # activity has nothing bounding how long or how many times it retries.\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n    )\\n    return result\\n\", \"solution\": \"import time\\nimport uuid\\nfrom datetime import datetime\\nfrom dataclasses import dataclass\\nfrom typing import Optional\\n\\n\\n@dataclass\\nclass RetryPolicy:\\n    initial_interval: float = 1.0\\n    backoff_coefficient: float = 2.0\\n    maximum_interval: Optional[float] = None\\n    maximum_attempts: int = 0\\n\\n\\nclass _FakeWorkflow:\\n    \\\"\\\"\\\"Deterministic stand-ins for wall-clock time, randomness and identity\\n    inside a workflow. A real SDK records the first result in history and\\n    serves that same result back on every replay, so calling these twice\\n    against the same history has to give the same answer both times.\\\"\\\"\\\"\\n\\n    def __init__(self, fixed_uuid=\\\"1111-aaaa-bbbb\\\", fixed_now=None, fixed_random=0.42):\\n        self._fixed_uuid = fixed_uuid\\n        self._fixed_now = fixed_now if fixed_now is not None else datetime(2024, 1, 1, 10, 0, 0)\\n        self._fixed_random = fixed_random\\n        self.log = []\\n\\n    def uuid4(self):\\n        self.log.append({\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": self._fixed_uuid})\\n        return self._fixed_uuid\\n\\n    def now(self):\\n        self.log.append({\\\"op\\\": \\\"now\\\", \\\"value\\\": self._fixed_now.isoformat()})\\n        return self._fixed_now\\n\\n    def random(self):\\n        self.log.append({\\\"op\\\": \\\"random\\\", \\\"value\\\": self._fixed_random})\\n        return self._fixed_random\\n\\n    def sleep(self, seconds):\\n        self.log.append({\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": seconds})\\n\\n    def execute_activity(self, fn, arg, start_to_close=None, retry_policy=None):\\n        self.log.append({\\n            \\\"op\\\": \\\"execute_activity\\\",\\n            \\\"activity\\\": getattr(fn, \\\"__name__\\\", str(fn)),\\n            \\\"arg\\\": arg,\\n            \\\"start_to_close\\\": start_to_close,\\n            \\\"maximum_attempts\\\": retry_policy.maximum_attempts if retry_policy else None,\\n        })\\n        return {\\\"status\\\": \\\"confirmed\\\", \\\"arg\\\": arg}\\n\\n\\ndef confirm_booking(details):\\n    \\\"\\\"\\\"The activity being called. This part is already correct, leave it alone.\\\"\\\"\\\"\\n    return {\\\"confirmed\\\": True, \\\"hotel\\\": details.get(\\\"hotel\\\")}\\n\\n\\nworkflow = _FakeWorkflow()\\n\\n\\ndef book_hotel_workflow(request):\\n    idem_key = str(workflow.uuid4())\\n\\n    if workflow.now().hour < 9:\\n        rate_plan = \\\"early_bird\\\"\\n    else:\\n        rate_plan = \\\"standard\\\"\\n\\n    workflow.sleep(0.05)\\n\\n    result = workflow.execute_activity(\\n        confirm_booking,\\n        {\\\"hotel\\\": request[\\\"hotel\\\"], \\\"idem_key\\\": idem_key, \\\"rate_plan\\\": rate_plan},\\n        start_to_close=10,\\n        retry_policy=RetryPolicy(\\n            initial_interval=1,\\n            backoff_coefficient=2,\\n            maximum_interval=30,\\n            maximum_attempts=5,\\n        ),\\n    )\\n    return result\\n\", \"tests\": \"class _BannedModule:\\n    \\\"\\\"\\\"Stands in for a banned standard-library module. Any attribute access or\\n    call raises with the message naming the replacement.\\\"\\\"\\\"\\n\\n    def __init__(self, message):\\n        self._message = message\\n\\n    def __getattr__(self, name):\\n        raise AssertionError(self._message)\\n\\n    def __call__(self, *args, **kwargs):\\n        raise AssertionError(self._message)\\n\\n\\nuuid = _BannedModule(\\n    \\\"book_hotel_workflow called uuid.uuid4() directly: a fresh id on every run \\\"\\n    \\\"means replay computes a different idempotency key than the one already in \\\"\\n    \\\"history, use workflow.uuid4() instead\\\"\\n)\\ndatetime = _BannedModule(\\n    \\\"book_hotel_workflow read the wall clock through datetime directly: the \\\"\\n    \\\"branch can take a different path on replay, use workflow.now() instead\\\"\\n)\\ntime = _BannedModule(\\n    \\\"book_hotel_workflow called time.sleep() directly: a real sleep blocks the \\\"\\n    \\\"worker and is not a recorded event, use workflow.sleep(seconds) instead\\\"\\n)\\nrandom = _BannedModule(\\n    \\\"book_hotel_workflow used random directly: the value would be recomputed on \\\"\\n    \\\"replay, use workflow.random() instead\\\"\\n)\\n\\nrequest = {\\\"hotel\\\": \\\"riverside-inn\\\"}\\n\\nworkflow.log = []\\nresult_1 = book_hotel_workflow(request)\\nlog_1 = list(workflow.log)\\n\\nworkflow.log = []\\nresult_2 = book_hotel_workflow(request)\\nlog_2 = list(workflow.log)\\n\\naudit_1 = audit_command_log(log_1)\\naudit_2 = audit_command_log(log_2)\\n\\nassert audit_1[\\\"op_sequence\\\"] == audit_2[\\\"op_sequence\\\"], (\\n    f\\\"run 1 emitted commands {audit_1['op_sequence']} but run 2 emitted \\\"\\n    f\\\"{audit_2['op_sequence']}: the same recorded history must produce the \\\"\\n    f\\\"same sequence of commands on replay\\\"\\n)\\n\\nassert log_1 == log_2, (\\n    f\\\"run 1 emitted {log_1} but run 2 emitted {log_2}: every recorded command \\\"\\n    f\\\"must carry the same values on replay\\\"\\n)\\n\\nassert audit_1[\\\"idem_key\\\"] == audit_2[\\\"idem_key\\\"], (\\n    f\\\"run 1 emitted idem_key={audit_1['idem_key']} but run 2 emitted \\\"\\n    f\\\"idem_key={audit_2['idem_key']}: the key must come from workflow.uuid4()\\\"\\n)\\n\\nassert \\\"uuid4\\\" in audit_1[\\\"op_sequence\\\"], (\\n    \\\"no uuid4 command was recorded: the idempotency key must come from \\\"\\n    \\\"workflow.uuid4() so it lands in history\\\"\\n)\\n\\nassert \\\"now\\\" in audit_1[\\\"op_sequence\\\"], (\\n    \\\"no now command was recorded: the rate-plan branch must read the clock \\\"\\n    \\\"through workflow.now()\\\"\\n)\\n\\nassert \\\"sleep\\\" in audit_1[\\\"op_sequence\\\"], (\\n    \\\"no sleep command was recorded: the delay must go through \\\"\\n    \\\"workflow.sleep(seconds) so it becomes a recorded timer\\\"\\n)\\n\\nassert audit_1[\\\"has_activity\\\"], \\\"book_hotel_workflow never called workflow.execute_activity\\\"\\n\\nassert audit_1[\\\"has_timeout\\\"], (\\n    \\\"the activity call has no start_to_close timeout: a stuck activity would \\\"\\n    \\\"retry forever with nothing to cut it off\\\"\\n)\\n\\nassert audit_1[\\\"has_bounded_retries\\\"], (\\n    f\\\"maximum_attempts={audit_1['maximum_attempts']}: pass a RetryPolicy with \\\"\\n    f\\\"maximum_attempts set so the retries are bounded\\\"\\n)\\n\\nassert result_1 == result_2, (\\n    f\\\"run 1 returned {result_1} but run 2 returned {result_2}: the workflow \\\"\\n    f\\\"result must be identical across replays\\\"\\n)\\n\", \"hints\": [\"Swap the standard-library calls for the SDK shims: `uuid.uuid4()` becomes `workflow.uuid4()`, `datetime.now()` becomes `workflow.now()`, `time.sleep(...)` becomes `workflow.sleep(...)`. Nothing about what the code computes changes, only where the value comes from.\", \"The `execute_activity` call needs two more keyword arguments: `start_to_close=<seconds>` and `retry_policy=RetryPolicy(maximum_attempts=<n>, ...)`. Without a timeout, a stuck activity has nothing bounding how long it runs or how many times it gets retried.\", \"Leave `_FakeWorkflow`, `RetryPolicy`, `confirm_booking` and the module-level `workflow = _FakeWorkflow()` exactly as given. Only `book_hotel_workflow`'s body should change.\", \"Run the function in your head against the same recorded history twice: if any line can produce a different value the second time, that line is the bug.\"]}, \"functions\": [{\"name\": \"audit_command_log\", \"source\": \"def audit_command_log(commands):\\n    \\\"\\\"\\\"Inspect a captured list of workflow commands for replay-safety signals.\\n\\n    `commands` is a list of dicts, each shaped like one of:\\n      {\\\"op\\\": \\\"uuid4\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"now\\\", \\\"value\\\": ...}\\n      {\\\"op\\\": \\\"sleep\\\", \\\"seconds\\\": ...}\\n      {\\\"op\\\": \\\"execute_activity\\\", \\\"activity\\\": ..., \\\"arg\\\": {...},\\n       \\\"start_to_close\\\": number or None, \\\"maximum_attempts\\\": int or None}\\n\\n    Returns a plain dict summarizing the sequence so a caller can assert on\\n    it without re-walking the raw log.\\n    \\\"\\\"\\\"\\n    op_sequence = []\\n    has_activity = False\\n    start_to_close = None\\n    maximum_attempts = None\\n    idem_key = None\\n\\n    for entry in commands:\\n        if not isinstance(entry, dict):\\n            continue\\n        op = entry.get(\\\"op\\\")\\n        op_sequence.append(op)\\n        if op == \\\"execute_activity\\\":\\n            has_activity = True\\n            start_to_close = entry.get(\\\"start_to_close\\\")\\n            maximum_attempts = entry.get(\\\"maximum_attempts\\\")\\n            arg = entry.get(\\\"arg\\\")\\n            if isinstance(arg, dict):\\n                idem_key = arg.get(\\\"idem_key\\\")\\n\\n    has_bounded_retries = isinstance(maximum_attempts, int) and maximum_attempts > 0\\n\\n    return {\\n        \\\"op_sequence\\\": op_sequence,\\n        \\\"has_activity\\\": has_activity,\\n        \\\"start_to_close\\\": start_to_close,\\n        \\\"maximum_attempts\\\": maximum_attempts,\\n        \\\"idem_key\\\": idem_key,\\n        \\\"has_timeout\\\": start_to_close is not None,\\n        \\\"has_bounded_retries\\\": has_bounded_retries,\\n    }\"}]}"
    }
  ]
}